# Phase-IV Provenance Split-Identity Remediation Authority-Semantics Correction / Existing Candidate-Delta Adoption Authority Candidate

## 1. Authority, phase, verdict, and scope

This is a REPORT-ONLY AUTHORITY CORRECTION / PROSPECTIVE EXISTING-DELTA ADOPTION candidate. It corrects the authority-activation semantics contradiction in current authority report commit `45e3f604113c429748e10ffc72d1270c497d459c`; it is governed by historical failed-execution authority `d6356bdb66e06e5569209a89a8c7a25ae439f728` and prior implementation anchor `25569c0234086fb05d1120a7b0b5490aa751c182` only as authenticated historical context.

Verdict: `PASS_READY_FOR_FRESH_INDEPENDENT_HIGH_RISK_PHASE_IV_REMEDIATION_AUTHORITY_SEMANTICS_CORRECTION_EXISTING_DELTA_ADOPTION_VERIFICATION`.

This candidate authorizes no implementation, execution, pytest, checker CLI, trainer, training, evaluation, producer/materialization, CUDA/GPU/Kaggle use, model/checkpoint loading, staging, commit, push, fetch, ref/config/cache/ACL/ignore mutation, or scientific action. It creates no scientific authority.

Its sole prospective subject is the exact unchanged two-file working-tree delta authenticated in Sections 5 through 7. The two files are not modified by authoring this report.

## 2. Opening repository state

Before this candidate was created, the required state was authenticated:

- Branch: `p3w7-a1-a2-a3-factorial-execution-authority-n3-v2`.
- HEAD: `45e3f604113c429748e10ffc72d1270c497d459c`.
- Configured upstream: `origin/p3w7-a1-a2-a3-factorial-execution-authority-n3-v2`.
- Upstream tip: `45e3f604113c429748e10ffc72d1270c497d459c`.
- Ahead/behind: `0/0`.
- Staged: `0`; untracked: `0` before candidate creation.
- Tracked modifications: exactly `scripts/validate_reason_router_p4x_prelaunch_static_control.py` and `tests/test_reason_router_p4x_prelaunch_static_control.py`.

No fetch or alteration of refs, configuration, cache, ACL, or ignore state was performed.

## 3. Current-authority authentication and contradiction

Commit `45e3f604113c429748e10ffc72d1270c497d459c` was authenticated with sole parent `d6356bdb66e06e5569209a89a8c7a25ae439f728`. Its sole changed path is `reports/reason_router_p3w7_p2_degeneracy_seed8192_revised_p4l_phase4_static_control_provenance_split_identity_mismatch_root_cause_remediation_authority_spec_candidate.md`, whose blob is `cb0b9d40dbaf9a5670de6d390da0f17e2a45c2ea`. That entire report was read.

The report has the exact authority defect `AUTHORITY_ACTIVATION_SEMANTICS_CONTRADICTION`: it describes a future bounded checker/test remediation lifecycle, while explicitly stating that it “does not authorize implementation.” The latter sentence controls this phase. Therefore `45e3f604113c429748e10ffc72d1270c497d459c` is not sufficient authority for the already-created working-tree implementation delta. This candidate does not reinterpret, silently override, or erase that sentence.

## 4. Existing-delta status and non-retroactivity

The present two-file working-tree delta is classified exactly as `PRE_EXISTING_UNCOMMITTED_UNEXECUTED_REMEDIATION_CANDIDATE_DELTA`.

It is not an authorized implementation freeze, executable evidence, committed evidence, or scientific evidence. Its creation is not retroactively authorized by this candidate, by `45e3f604113c429748e10ffc72d1270c497d459c`, or by any historical report. No byte of either candidate file may change during this report-authoring phase.

## 5. Exact checker raw-byte and blob identity

Path: `scripts/validate_reason_router_p4x_prelaunch_static_control.py`.

| Property | Authenticated value |
| --- | --- |
| SHA256 | `bcbf1818cfed1351077d2f3c2db809a70cc6953ef2d50adaa08d5b9e49a409b3` |
| Bytes | `25070` |
| LF / CR / CRLF | `359 / 0 / 0` |
| BOM | absent |
| Final LF | present |
| Trailing-whitespace lines | `0` |
| Git blob | `c49725202aac50e65b8b3dd7a1e0cbe53484047e` |

The raw-byte Git blob SHA-1 was independently calculated using the Git blob header and equals `c49725202aac50e65b8b3dd7a1e0cbe53484047e`; `git hash-object --no-filters` independently returns the same object ID.

## 6. Exact test raw-byte and blob identity

Path: `tests/test_reason_router_p4x_prelaunch_static_control.py`.

| Property | Authenticated value |
| --- | --- |
| SHA256 | `98fe97715a8b0f67cdb04b4ea8e2a0da5fbf3fe525d8ea5ef532e44ef59e61b5` |
| Bytes | `33250` |
| LF / CR / CRLF | `496 / 0 / 0` |
| BOM | absent |
| Final LF | present |
| Trailing-whitespace lines | `0` |
| Git blob | `f9c581d29c4333e55e32dbe8828c1730706be45a` |

The raw-byte Git blob SHA-1 was independently calculated using the Git blob header and equals `f9c581d29c4333e55e32dbe8828c1730706be45a`; `git hash-object --no-filters` independently returns the same object ID.

## 7. Exact candidate-delta diff identity

The exact modified fileset has no third path. Its per-file diff statistics are:

| File | Additions | Deletions |
| --- | ---: | ---: |
| `scripts/validate_reason_router_p4x_prelaunch_static_control.py` | `2` | `1` |
| `tests/test_reason_router_p4x_prelaunch_static_control.py` | `35` | `3` |
| Total | `37` | `4` |

Raw stdout from exactly `git diff --binary --full-index --no-ext-diff` was captured as bytes directly from the subprocess stdout stream, without PowerShell string conversion, Unicode decode/re-encode, line joining, or normalization. It has `13543` bytes, SHA256 `f4cb1d5a57a43cc79a3cde1067c87704fb49e15302f6a5f8f4b663aa89335173`, and is LF-only (`84` LF, `0` CR, `0` CRLF).

The prior `c4c1a5a539935bbd803474a73572af2904b9b49f5cb70c326c3a071add10ef1b` is classified as `NON_AUTHORITATIVE_TEXT_NORMALIZED_OR_OTHERWISE_NON_RAW_DIFF_IDENTITY`. The required raw stream identity `f4cb1d5a57a43cc79a3cde1067c87704fb49e15302f6a5f8f4b663aa89335173` is classified as `AUTHORITATIVE_RAW_GIT_DIFF_BYTE_IDENTITY`.

## 8. Static semantic review of the exact candidate delta

Without executing changed bytes, static source and diff inspection confirms all of the following for the exact authenticated bytes:

- `SPLIT_IDENTITIES` is unchanged, and `recompute_split()` retains the exact `audit == SPLIT_IDENTITIES` current-split contract.
- A separate `PROVENANCE_SPLIT_IDENTITIES` contract is defined as the exact eleven current keys plus only `historical_seed174_dev_pair_sha256` with value `259bfce57e85121d6c1adccd20f3ac070108ff6310cfff546a2edd054835899d`.
- `_validate_provenance()` uses exact-dict equality against that separate provenance contract, so missing, altered, malformed, and unknown extra keys fail closed.
- The failure name `P4X_PROVENANCE_SPLIT_IDENTITY_MISMATCH` is preserved.
- The test delta includes a real frozen-provenance positive focused case, verifies the eleven-plus-historical inventory and exact historical value, and invokes `_validate_provenance()` on that real artifact.
- The focused test delta covers all required five negative mutation classes: removal and alteration of a current key, removal and alteration of the historical key, and an unknown extra key; each requires the preserved named failure.
- Synthetic provenance fixtures are adapted to the separate provenance contract.
- The historical Seed174 rejection is preserved: the historical identity is absent from `SPLIT_IDENTITIES`, while present only in the exact provenance contract.

No code change is required to write this authority candidate. The static review establishes only the bounded intended remediation correspondence; it does not establish executable evidence.

## 9. Prospective adoption authority and binding

This candidate does not retroactively authorize creation of the existing delta. If and only if this successor report is independently high-risk verified, byte/blob frozen, activated in a dedicated report-only commit, pushed, and remotely verified, that activation prospectively authorizes treating precisely the unchanged checker and test bytes in Sections 5 through 7 as the bounded remediation candidate.

After that activation, the two files may remain unchanged; no rewrite is required merely to recreate an authority chronology. Any change to either bound file byte, its authenticated raw identity/blob, the exact two-file fileset, or the raw diff binding invalidates this adoption binding and requires separately authorized implementation work. This is prospective adoption of exact existing bytes, not retroactive authorization.

## 10. Execution prohibition and required later lifecycle

Even after a future activation of this successor authority, it does not authorize pytest, the checker CLI, trainer, training/evaluation, producer/materialization, CUDA/GPU/Kaggle, or model/checkpoint load.

After adoption, the exact next technical phase is fresh independent high-risk implementation verification against the newly activated adoption authority, then implementation byte/diff freeze and an implementation freeze commit/push. Only after that, a new execution-validation authority is required before pytest or checker execution. No prior execution authority may be reused for changed or adopted bytes.

## 11. Preserved scientific and production boundaries

This candidate authorizes no change to the dataset, Seed8192 split, `SPLIT_IDENTITIES` values, provenance artifact, sidecar, Phase-II evidence, execution record, trainer/rebind, labels, loss, gradient ownership, EMA, calibration, cohorts, aggregates, historical Seed174 rejection, branch/upstream contracts, implementation-anchor semantics, symlink defense, or `P4X_UNTRACKED_INPUT` remediation. It creates no scientific authority and does not alter data, label, split, or promotion semantics.

## 12. Candidate integrity, post-authoring state, and next action

This candidate must be UTF-8 without BOM, LF-only, final-LF terminated, and have zero trailing-whitespace lines. Its final raw SHA256, byte count, line-ending counts, predicted Git blob, and manual Git blob SHA-1 are deliberately not embedded here and must be measured after authoring.

After candidate creation, required repository state is HEAD `45e3f604113c429748e10ffc72d1270c497d459c`; tracked modifications exactly the checker and test; staged `0`; untracked exactly this one successor report; and no other path. No staging, commit, or push is authorized in this phase.

Exact next authorized action: `FRESH_INDEPENDENT_HIGH_RISK_PHASE_IV_REMEDIATION_AUTHORITY_SEMANTICS_CORRECTION_EXISTING_DELTA_ADOPTION_VERIFICATION`.
