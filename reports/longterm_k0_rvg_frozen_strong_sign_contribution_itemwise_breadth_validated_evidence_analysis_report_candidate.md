# ContraMamba K0-RVG Frozen-Strong Sign-Contribution Itemwise Breadth / Consistency
## Validated Evidence Analysis Report Candidate

## 1. Status

**Stage:** validated K0 scientific static analysis.

**Scientific question:**

Is the validated population-level positive co-contribution dominance broadly expressed across the frozen common-330 items, or is the positive population mean produced by a minority of large-effect items despite substantial itemwise heterogeneity?

**Validated primary classification:**

**A1/B1 — broad positive total with majority parent-component concordance.**

This means:

- the net strong-subspace interaction contrast is positive for a large majority of frozen items;
- the population-level positive-co-contribution-dominant structure is also the majority itemwise organization;
- the parent result is therefore not merely a small-minority positive tail effect.

This remains an observational/algebraic static localization.

It is not a causal intervention result.

---

## 2. Authority chain

### Static design freeze

Commit:

`f34ad21beeda00e94904844375839f37c0fbee67`

File:

`reports/longterm_k0_rvg_frozen_strong_sign_contribution_itemwise_breadth_static_design_candidate.md`

SHA256:

`8a7f6fcf85dc8954379699198ac5336bef54e86d7dad77402a9ed4d727575af5`

Git blob:

`43132b38894f475c1f1755adb4e0a244a9636d18`

### Static analyzer implementation freeze

Commit:

`8df86fa8d0302d9e97ac31e41190b3a613c76896`

File:

`scripts/longterm_k0_rvg_frozen_strong_sign_contribution_itemwise_breadth_static_analysis.py`

SHA256:

`c988c64df8fdd7af9e55e2ce83bf7054e19160f1c898a19152bbed250cb63538`

---

## 3. Immediate parent evidence

Parent evidence freeze:

`3f7a9eaa38935fc066f20f81cbce0a9f901909a9`

Parent implementation:

`3af43c91d791e2755b977529eb26d65db00c7870`

Parent scientific conclusion:

**Outcome A — positive co-contribution dominant.**

Parent population decomposition:

`G_total = +0.760594723140676`

`G_pos = +0.5314816993853949`

`G_relief = +0.22911302375528114`

Absolute attribution:

- positive co-contribution gain:
  `69.87712157544046%`;
- cancellation relief:
  `30.12287842455955%`.

The present stage does not replace that result.

It tests how broadly that result is expressed itemwise across the fixed common-330 cohort.

---

## 4. Frozen parent artifacts

### Item metrics

Path:

`reports/longterm_k0_rvg_frozen_strong_alignment_sign_contribution_3af43c9_v1/frozen_strong_alignment_sign_contribution_item_metrics.jsonl`

SHA256:

`cd9d3306c7ef2b2d8f2a759be5e7945ccfdb86f0009981f61247b9ddfcf03974`

Rows:

`330`

### Parent summary

Path:

`reports/longterm_k0_rvg_frozen_strong_alignment_sign_contribution_3af43c9_v1/summary.json`

SHA256:

`0696e5887dfbd2fc7eb55d3c726e4e55415858e15522d52efca6ce38ecda1615`

### Parent static-analysis manifest

Path:

`reports/longterm_k0_rvg_frozen_strong_alignment_sign_contribution_3af43c9_v1/static_analysis_manifest.json`

SHA256:

`5e1fdd97236d0e6f2d7b7041207d25af78aba7dae3b15cff9a66d760c365d50f`

### Parent validated report

Path:

`reports/longterm_k0_rvg_frozen_strong_alignment_sign_contribution_validated_evidence_analysis_report_candidate.md`

SHA256:

`a8e4e7fe528e9b01a16b26d7d3508938d7f63bbd4cc57153207c9ae4c58767d6`

---

## 5. Frozen population and scope

The present stage preserves exactly:

- common DDSSSSS cohort:
  `330`;
- current token only;
- relative coordinate:
  `k=2`;
- frozen strong partition:
  `240` channels;
- source block:
  `20`;
- target residual layer:
  `21`;
- downstream parent map:
  layer `22`;
- existing matched/swapped role definitions;
- existing sign-contribution decomposition.

No item subgroup was selected.

No channel subset was changed.

No threshold was fitted from results.

The majority boundary:

`165`

is exactly half of the fixed 330-item cohort.

---

## 6. Execution boundary

The validated execution is completely model-free.

Execution flags:

- `new_model_execution = False`;
- `model_forward_count = 0`;
- `checkpoint_loaded = False`;
- `handoff_opened = False`;
- `transformers_imported = False`;
- `tokenizer_invoked = False`;
- `logits_read = False`;
- `task_heads_executed = False`;
- `training_executed = False`;
- `causal_intervention_executed = False`;
- `pca_svd_or_learned_geometry_executed = False`;
- `posthoc_subset_search_executed = False`;
- `new_item_selection = False`;
- `new_channel_selection = False`;
- `raw_vectors_read_or_persisted = False`.

All new results are deterministic transforms of frozen parent scalar evidence.

---

## 7. Item-level definitions

For frozen item `i`:

`p_i = ΔP_i`

is positive co-contribution gain.

`r_i = ΔN_i`

is cancellation relief when positive.

`t_i = ΔI_i`

is total strong-subspace interaction contrast.

Exact identity:

`t_i = p_i + r_i`.

Count/strength diagnostics:

`c_i = Δn_same`

`o_i = Δn_opp`

`a_i = ΔPbar`

`k_i = ΔKbar`.

Exact count identity:

`c_i + o_i = 0`.

---

## 8. Primary two-axis classification

### Axis A — total-effect breadth

Positive total items:

`N_total_pos = 295`

Fraction:

`F_total_pos = 0.8939393939393939`

or:

`89.39393939393939%`.

Negative total items:

`35`.

Exact-zero total items:

`0`.

Since:

`295 > 165`

the validated Axis A result is:

**A1 — majority-broad positive total.**

The parent positive population mean is therefore not minority-driven in sign.

---

## 9. Axis B — parent-component concordance

A parent-concordant item is defined as:

`t_i > 0`

`p_i > 0`

and:

`|p_i| > |r_i|`.

Validated count:

`N_parent_concordant = 236`

Fraction:

`F_parent_concordant = 0.7151515151515152`

or:

`71.51515151515152%`.

Since:

`236 > 165`

the validated Axis B result is:

**B1 — majority parent-component concordance.**

Thus a majority of items not only have positive total effect, but also have positive co-contribution as the larger absolute signed component.

---

## 10. Combined primary result

Combining Axis A and Axis B:

**A1/B1 — broad positive total with majority parent-component concordance.**

Exact implementation label:

`A1/B1_BROAD_POSITIVE_TOTAL_WITH_MAJORITY_PARENT_CONCORDANCE`

This is the primary scientific classification of the stage.

---

## 11. Positive-component sign breadth

Items with:

`p_i > 0`

count:

`274`

fraction:

`274 / 330 = 0.8303030303030303`

or:

`83.03030303030303%`.

Items with:

`p_i < 0`

count:

`56`.

Exact-zero:

`0`.

Therefore the population-level positive co-contribution gain is itself broadly positive itemwise.

---

## 12. Cancellation-relief sign breadth

Items with:

`r_i > 0`

count:

`307`

fraction:

`307 / 330 = 0.9303030303030303`

or:

`93.03030303030303%`.

Items with:

`r_i < 0`

count:

`23`.

Exact-zero:

`0`.

Thus cancellation relief is even more sign-consistent across items than the positive co-contribution component.

This does not make it the dominant population component.

It means its direction is more uniformly positive itemwise.

---

## 13. Both-components-reinforce breadth

Items with:

`p_i > 0`

and:

`r_i > 0`

count:

`256`.

Fraction:

`256 / 330 = 0.7757575757575758`

or:

`77.57575757575758%`.

Therefore the parent statement that both components reinforce the positive total is not merely an aggregate artifact.

It holds simultaneously on more than three quarters of frozen items.

---

## 14. Component sign quadrants

Validated counts:

### Q++

`p_i > 0`, `r_i > 0`

count:

`256`.

### Q+-

`p_i > 0`, `r_i < 0`

count:

`18`.

### Q-+

`p_i < 0`, `r_i > 0`

count:

`51`.

### Q--

`p_i < 0`, `r_i < 0`

count:

`5`.

### Boundary

count:

`0`.

All quadrant counts sum exactly to:

`330`.

The dominant quadrant is therefore:

**Q++ — both components reinforce corr.**

---

## 15. Itemwise component dominance

Positive-component dominant items:

`270`.

Fraction:

`270 / 330 = 0.8181818181818182`

or:

`81.81818181818181%`.

Cancellation-relief dominant items:

`60`.

Exact ties:

`0`.

Therefore the parent population result:

**positive co-contribution dominant**

is also the dominant itemwise component relation for more than four-fifths of the cohort.

This is stronger evidence than population mean dominance alone.

---

## 16. Same-sign participation-count breadth

Items with:

`Δn_same > 0`

count:

`200`.

Fraction:

`0.6060606060606061`

or:

`60.60606060606061%`.

Items with:

`Δn_same < 0`

count:

`118`.

Exact zero:

`12`.

This direction is only modestly majority-broad.

Therefore same-sign participation count is substantially less itemwise consistent than the main signed-mass phenotype.

---

## 17. Positive per-channel strength breadth

Items with:

`ΔPbar > 0`

count:

`277`.

Fraction:

`0.8393939393939394`

or:

`83.93939393939394%`.

Items with:

`ΔPbar < 0`

count:

`53`.

Exact zero:

`0`.

This is considerably broader than the count direction.

The positive-side parent phenotype is therefore much more consistently expressed through per-same-sign-channel positive strength than through increased same-sign channel count.

---

## 18. Cancellation per-channel strength breadth

Because:

`ΔKbar < 0`

means weaker cancellation magnitude for corr, validated counts are:

relief direction:

`313`.

Fraction:

`0.9484848484848485`

or:

`94.84848484848484%`.

Worse direction:

`17`.

Exact zero:

`0`.

This is the single most itemwise-consistent count/strength diagnostic in the stage.

Therefore the secondary cancellation-relief phenotype is extremely broad in per-opposite-sign-channel strength direction.

---

## 19. Positive-side joint count/strength patterns

Validated positive-side joint patterns:

### More same-sign channels + stronger positive per-channel mass

`C+/A+ = 179`

### More same-sign channels + weaker positive per-channel mass

`C+/A- = 21`

### Fewer same-sign channels + stronger positive per-channel mass

`C-/A+ = 89`

### Fewer same-sign channels + weaker positive per-channel mass

`C-/A- = 29`

### Count boundary

`12`.

The dominant joint pattern is:

`C+/A+`

with:

`179 / 330 = 54.24242424242424%`.

However a substantial:

`89 / 330 = 26.96969696969697%`

show:

`C-/A+`.

This is important.

It means positive per-channel strength frequently remains supportive even when the same-sign participation count moves in the opposite direction.

Hence the broad positive-side signal cannot be reduced to count gain alone.

---

## 20. Cancellation-side joint count/strength patterns

Validated cancellation-side joint patterns:

### Fewer opposite-sign channels + weaker cancellation per channel

`O_RELIEF/K_RELIEF = 189`

### Fewer opposite-sign channels + worse cancellation per channel

`O_RELIEF/K_WORSE = 11`

### More opposite-sign channels + weaker cancellation per channel

`O_WORSE/K_RELIEF = 113`

### More opposite-sign channels + worse cancellation per channel

`O_WORSE/K_WORSE = 5`

### Count boundary

`12`.

The dominant pattern is:

`O_RELIEF/K_RELIEF = 189`.

But another large group:

`113`

has worse count direction yet still has per-channel cancellation relief.

Again, per-channel strength direction is more stable than participation count direction.

---

## 21. Cross-item mass balance — total effect

Positive itemwise total mass:

`262.45341369805647`.

Negative itemwise total mass:

`-11.45715506163341`.

Net:

`250.99625863642305`.

Negative-to-positive absolute mass ratio:

`0.04365405235237088`.

Thus negative-total items contribute only about:

`4.365405235237088%`

as much absolute mass as positive-total items.

This further rejects a picture in which the positive mean is precariously balanced against a large opposing item population.

---

## 22. Cross-item mass balance — positive component

Positive `p_i` mass:

`192.85357586347087`.

Negative `p_i` mass:

`-17.464615066290563`.

Net:

`175.3889607971803`.

Negative-to-positive absolute ratio:

`0.0905589382415937`.

Thus negative positive-component items offset only about:

`9.05589382415937%`

of the positive positive-component mass.

---

## 23. Cross-item mass balance — cancellation relief

Positive `r_i` mass:

`77.04498857993954`.

Negative `r_i` mass:

`-1.4376907406967612`.

Net:

`75.60729783924278`.

Negative-to-positive absolute ratio:

`0.018660405656430942`.

Thus opposing cancellation-relief mass is extremely small relative to reinforcing cancellation-relief mass.

---

## 24. Relation to parent population means

The validated breadth analysis reproduces the parent means:

`mean(t_i) = +0.760594723140676`

`mean(p_i) = +0.5314816993853949`

`mean(r_i) = +0.22911302375528114`

`mean(c_i) = +4.551515151515152`

`mean(a_i) = +0.0035643520982118104`

`mean(k_i) = -0.001921654940814375`.

Thus the breadth conclusions are directly linked to, and consistent with, the frozen parent population decomposition.

---

## 25. Parent median bridges

The analyzer also reproduced parent medians:

`median(t_i) = +0.7522478498155687`

`median(p_i) = +0.5693259571352562`

`median(r_i) = +0.19919127457024965`

`median(c_i) = +3.0`

`median(a_i) = +0.0036918390252776183`

`median(k_i) = -0.0017159590409509089`.

Positive medians for:

`t_i`, `p_i`, `r_i`, `c_i`, `a_i`

and negative median for:

`k_i`

are consistent with the majority-direction counts.

---

## 26. Exact identity validation

Maximum paired identity residual:

`1.1657341758564144e-15`.

Maximum count identity residual:

`0.0`.

Maximum parent reproduction residual:

`0.0`.

All are far below the frozen absolute tolerance:

`5e-12`.

No row was removed.

No failed row was masked.

---

## 27. Static execution

Run directory:

`reports/longterm_k0_rvg_frozen_strong_sign_contribution_itemwise_breadth_8df86fa_v1`

Execution marker:

`PASS_FROZEN_STRONG_SIGN_CONTRIBUTION_ITEMWISE_BREADTH_STATIC_ANALYSIS`

Execution HEAD:

`8df86fa8d0302d9e97ac31e41190b3a613c76896`

Model forward count:

`0`.

---

## 28. Independent artifact validation

External validator:

`validate_frozen_strong_sign_contribution_itemwise_breadth_artifacts.py`

Validator SHA256:

`f6b491aa8580018fb1bfb052d687eada48608baa259c49a357aa58839fe11db3`

Validation marker:

`PASS_FROZEN_STRONG_SIGN_CONTRIBUTION_ITEMWISE_BREADTH_ARTIFACT_VALIDATION`

The validator independently checked:

- frozen analysis HEAD;
- analyzer SHA;
- authority freeze;
- parent evidence freeze;
- all four parent artifact SHA256 identities;
- output file hashes;
- all 330 classification rows;
- item identity closure;
- count identity closure;
- quadrant classification;
- component dominance;
- parent-concordance;
- population means;
- parent medians;
- primary `A1/B1` classification.

---

## 29. Validated output identities

### Item classification

File:

`frozen_strong_sign_contribution_itemwise_breadth_classification.jsonl`

SHA256:

`c5d8f5019973458b1c22892536eaf9515fd840607ec7b39c935d7792ae970fd7`

Rows:

`330`.

### Summary

File:

`summary.json`

SHA256:

`4cbe22ea0e2355632347102fc47f72205b734ed0e5c0008869805fdadfa776bb`

### Static-analysis manifest

File:

`static_analysis_manifest.json`

SHA256:

`bfd351bdfd6a34658d79982967c0eb1f726844f0f920d1c802c0f333e85c6c8a`

---

## 30. Code correctness

Code correctness evidence consists of:

1. frozen static design at:
   `f34ad21beeda00e94904844375839f37c0fbee67`;
2. frozen analyzer implementation at:
   `8df86fa8d0302d9e97ac31e41190b3a613c76896`;
3. static preflight PASS;
4. exact parent artifact authentication;
5. exact item and count identities;
6. exact mean and median bridges;
7. independent external validator PASS.

No code correctness blocker remains for this stage.

---

## 31. Execution success

The static execution completed with:

`PASS_FROZEN_STRONG_SIGN_CONTRIBUTION_ITEMWISE_BREADTH_STATIC_ANALYSIS`.

It processed:

`330`

items.

No model forward occurred.

No checkpoint or handoff was opened.

Execution success is established.

---

## 32. Artifact / provenance validity

Independent artifact validation passed.

The external validator confirmed:

- analysis HEAD;
- analyzer SHA;
- authority identity;
- parent evidence identity;
- parent artifact identities;
- output hashes;
- item classifications;
- primary classification;
- count/strength breadth;
- exact numerical bridges.

Artifact/provenance validity is established.

---

## 33. Scientific conclusion

With code correctness, execution success, and artifact/provenance validity established, this stage supports the validated conclusion:

**The frozen strong-subspace positive interaction phenotype is broadly expressed across the common-330 cohort rather than being driven by a small minority of extreme items. The net interaction is positive for 295/330 items (89.39%), and 236/330 items (71.52%) are parent-concordant positive-co-contribution-led cases.**

The parent population conclusion:

**positive co-contribution dominant with secondary cancellation relief**

is therefore not merely an aggregate mean effect.

It has broad itemwise support.

---

## 34. Stronger refinement: dominance breadth

Positive co-contribution is the larger absolute component for:

`270/330 = 81.82%`

of items.

This establishes that positive-component dominance itself is broad, independent of the stricter parent-concordance definition.

Therefore the validated parent component ordering is not restricted to a small subset.

---

## 35. Stronger refinement: reinforcing quadrant breadth

Both components reinforce corr for:

`256/330 = 77.58%`

of items.

Thus the population statement that both components reinforce the positive total is also broadly realized itemwise.

The main deviation from the Q++ pattern is:

`Q-+ = 51`

rather than:

`Q+- = 18`.

So when one component opposes the parent direction, it is more often the positive co-contribution component that is negative while cancellation relief remains positive.

This is a descriptive pattern only.

---

## 36. Count-versus-strength breadth distinction

The count effect is substantially less consistent than the per-channel strength effects.

Same-sign count support:

`200/330 = 60.61%`.

Positive per-channel strength support:

`277/330 = 83.94%`.

Cancellation per-channel strength relief:

`313/330 = 94.85%`.

Therefore the broad itemwise structure is better described as:

**strength-consistent with only moderate count consistency**

rather than:

**uniform count expansion**.

This distinction materially refines the previous parent interpretation.

---

## 37. Validated refined scientific statement

A concise validated description is:

**Broad frozen-strong positive-co-contribution dominance with majority parent concordance, broad dual-component reinforcement, and markedly stronger itemwise consistency in per-channel strength than in participation count.**

An expanded version is:

**Across the fixed common-330 cohort, the positive strong-subspace interaction advantage is broadly present and usually positive-co-contribution-led. Cancellation relief is even more sign-consistent itemwise, but remains secondary in population magnitude. Participation-count shifts are comparatively heterogeneous, while positive per-channel contribution strength and reduced per-channel cancellation are broadly consistent.**

---

## 38. What the result rules out

The validated evidence rules out the narrow interpretation that the parent positive mean is primarily produced by a small minority of large positive-effect items.

It also rules out the interpretation that positive-component dominance exists only at the aggregate mean level.

It does not rule out item heterogeneity.

There remain:

- `35` negative-total items;
- `56` negative-positive-component items;
- `23` negative-cancellation-relief items;
- `60` cancellation-relief-dominant items.

Therefore the phenotype is broad, not universal.

---

## 39. Scientific limits

This stage does not establish:

- causality;
- necessity;
- sufficiency;
- that changing count changes the signed mass;
- that changing per-channel strength changes the downstream decision;
- that the pattern generalizes outside the common-330 cohort;
- that it generalizes outside `k=2`;
- that it generalizes outside the frozen strong-240 partition;
- that the same structure holds at another layer;
- that negative-total items define a distinct mechanistic subtype;
- that any quadrant should be promoted as a new subgroup.

No intervention was performed.

---

## 40. No post-hoc subgroup promotion

The stage used every frozen item.

No item was removed.

No top-k item subset was selected.

No error subgroup was selected.

No quadrant was promoted as a new scientific population.

Any future subgroup-specific mechanism analysis would require a new frozen design.

---

## 41. No K1 transition

This stage remains fully within K0-RVG.

The unrelated K1 files remain out of scope.

No K1 result follows from this evidence.

---

## 42. Decision value for next K0 step

The result is:

`A1/B1`.

Therefore the K0 chain has established that the parent signed-interaction phenotype is both:

1. broad in net effect;
2. broadly concordant with positive-co-contribution dominance.

This removes the main scientific reason to prioritize item-heterogeneity explanation before further localization.

A subsequent K0 channel-distribution analysis is therefore scientifically justified, provided it remains frozen-population, non-post-hoc, and model-free where possible.

---

## 43. Final classification

**Validated primary outcome:**

`A1/B1_BROAD_POSITIVE_TOTAL_WITH_MAJORITY_PARENT_CONCORDANCE`

Key counts:

- positive total:
  `295/330`;
- parent-concordant:
  `236/330`;
- positive `ΔP`:
  `274/330`;
- positive `ΔN`:
  `307/330`;
- both components reinforce:
  `256/330`;
- positive-component dominant:
  `270/330`;
- same-sign count positive:
  `200/330`;
- positive per-channel strength positive:
  `277/330`;
- cancellation per-channel relief:
  `313/330`.

Final validated descriptive conclusion:

**The frozen strong-subspace signed-interaction phenotype is broad rather than minority-driven, is majority parent-concordant and positive-co-contribution-led, and shows stronger itemwise consistency in per-channel strength than in participation count.**
