# K0-RVG Post-P2 Active-Encoding Token-Coordinate Equivalence Implementation Authority

**Status:** implementation and synthetic-validation authority candidate only.

**Date:** 2026-09-12

This authority defines the minimal successor provenance criterion for the
post-P2 token-window scientific question.

It does not authorize the real 336-item equivalence audit and does not
authorize the post-P2 token-window scientific audit.

## 1. Active line and parent state

`ACTIVE_SCIENTIFIC_LINE = K0_RVG_RAW_NATIVE_VECTOR_GEOMETRY`

`PARENT_HEAD = 4ce8ad7f355fba458f972bb44e005eeda7ca8193`

`POST_P2_OBJECTIVE = DISTINCT_TO_IDENTITY_TRANSITION_LOCALIZATION`

`POST_P2_PRIMARY_UNKNOWN = MATCHED_SWAPPED_TOKEN_WINDOW_RELATION`

The frozen P1 conclusion remains:

`RAW_NATIVE_VECTOR_ORGANIZATION_NOT_ESTABLISHED`

No scientific conclusion is changed by this authority.

## 2. Historical-byte recovery closure

The following status is permanent for this research line unless genuinely
new frozen evidence is discovered independently:

`HISTORICAL_P1_BYTE_IDENTITY_STATUS = HISTORICAL_P1_BYTE_IDENTITY_NOT_ESTABLISHED`

The bounded repository-evidence recovery result is:

`HISTORICAL_P1_BYTE_IDENTITY_RECOVERY = NOT_RECOVERABLE_FROM_FROZEN_REPOSITORY_EVIDENCE`

Exact historical tokenizer/config bytes are not required for the next
scientific estimand:

`HISTORICAL_P1_BYTE_IDENTITY_NECESSARY_FOR_NEXT_SCIENCE = NO`

No further historical-byte provenance archaeology is authorized here.

Any diverged-lineage precedent, including `46f97ed...`, is context only and
is not authority for this work.

`DIVERGED_LINEAGE_PRECEDENT_IS_AUTHORITY = NO`

## 3. Why a successor criterion is scientifically permissible

The next estimand concerns the matched-versus-swapped token relation under
the frozen branch construction and event-relative coordinates.

The required provenance question is therefore whether one exact active
encoding contract reproduces every token-coordinate invariant that is
actually persisted in the frozen P0 contract.

This authority does NOT claim that the active tokenizer bytes are the
historical P1 tokenizer bytes.

This authority does NOT claim that the full active branch token sequences
have been proven identical to unavailable historical P1 full branch token
sequences.

Instead:

`SUCCESSOR_PROVENANCE_TARGET = EXACT_ACTIVE_ENCODING_TOKEN_COORDINATE_CONTRACT_EQUIVALENCE`

`DOES_NOT_ESTABLISH_HISTORICAL_P1_FULL_TOKEN_SEQUENCE_IDENTITY = YES`

`DOES_NOT_ESTABLISH_HISTORICAL_P1_TOKENIZER_BYTE_IDENTITY = YES`

If and only if the later real equivalence audit passes every exact frozen
contract check, historical tokenizer-byte identity may be superseded as a
prerequisite for the next token-window science:

`HISTORICAL_BYTE_IDENTITY_PREREQUISITE_SUPERSEDABLE_FOR_NEXT_TOKEN_WINDOW_SCIENCE = YES`

No tolerance, similarity threshold, majority criterion, or post-hoc repair
is allowed.

`EQUIVALENCE_TOLERANCE_ALLOWED = NO`

## 4. Frozen active-encoding byte identity

The already-recorded active local tokenizer/config manifest is:

`ACTIVE_TOKENIZER_MODEL_ID = state-spaces/mamba-130m-hf`

`ACTIVE_TOKENIZER_REVISION_LABEL = 5708daa364c50b880e7bd92eab456e0d34492ee9`

`ACTIVE_TOKENIZER_TRANSFORMERS_VERSION = 5.12.1`

`ACTIVE_TOKENIZER_SNAPSHOT_MANIFEST_SHA256 = f45af0fad1ae940487eb6461c65f13cf1634b6141f4e4e03b6c88bc38fa6db9c`

The manifest artifact was frozen at:

`ACTIVE_TOKENIZER_MANIFEST_ARTIFACT_COMMIT = ea3a8469b658b5873c29d484ecbc9650c18841ad`

`ACTIVE_TOKENIZER_MANIFEST_ARTIFACT_SHA256 = bcd7fe3382a2bbbad5c176e18d66fa17f00dff0996fc6eefd51dcd7ecf83290f`

Its provenance verdict remains:

`ACTIVE_TOKENIZER_MANIFEST_PROVENANCE_VERDICT = PARTIAL_LOCAL_SNAPSHOT_PROVENANCE`

The later blob-match diagnostic was frozen at:

`LOCAL_BLOB_MATCH_BLOCKER_COMMIT = 4ce8ad7f355fba458f972bb44e005eeda7ca8193`

and established:

`LOCAL_CONTENT_ADDRESSED_BLOB_MATCH = 0_OF_4`

That blocker is not erased or reinterpreted by this authority.

The manifest digest above identifies the exact active encoding bytes to be
tested; it is not evidence of historical-byte identity.

## 5. Frozen P0 evidence

A later real equivalence audit must authenticate the frozen P0 archive
before reading scientific text.

Required SHA256 identities:

`P0_CANDIDATE_POOL_SHA256 = 743657411af4e143931e4d2c79f17043134bff3a370d30910588504c7f19f246`

`P0_GENERATED_SOURCE_SHA256 = 8137c0020a040faaf0c6be833b123e143dc5a0a09ae092e8e99530bb8671c1bf`

`P0_PHASE_PAIR_MAPPING_SHA256 = c5e1fa2ac946d153821896d5153354fae6e17038a55be87b3d31ab43d2921eda`

`P0_TOKEN_CONTRACTS_SHA256 = 6eb006f7deca28affa73318887421879e1279fd6897fab857b460873add9a998`

`P0_PROVISIONING_MANIFEST_SHA256 = feab9c60e3546ace3258f068e38d5bb577fc63b803b95349379fa8b4db425e52`

`P0_VALIDATION_REPORT_SHA256 = ae2fe4d1db13e1765415eab9c95263aec618fccf9299fe93fad0c8e31141b73a`

`EXPECTED_ITEM_COUNT = 336`

`EXPECTED_PHASE_PAIR_COUNT = 168`

## 6. Exact frozen branch construction

The equivalence implementation must reconstruct exactly:

`matched_corr = local prefix + local correction`

`matched_ctrl = local prefix + local control`

`swapped_corr = local prefix + phase-mate correction`

`swapped_ctrl = local prefix + phase-mate control`

No normalization, whitespace repair, Unicode normalization, trimming,
template rewriting, or alternative concatenation is allowed.

`BRANCH_TEXT_NORMALIZATION_ALLOWED = NO`

## 7. Exact equivalence conditions

For every one of the 336 items, a future real equivalence audit must
fail closed unless all persisted frozen token-coordinate invariants match
exactly.

Required checks include:

1. exact prefix token count;
2. exact prefix token-ID canonical SHA256;
3. exact matched-correction total token count;
4. exact matched-control total token count;
5. exact swapped-correction total token count;
6. exact swapped-control total token count;
7. exact matched correction-versus-control divergence anchor `t_e`;
8. exact swapped correction-versus-control divergence anchor `t_e`;
9. exact persisted divergence offsets from prefix where present;
10. exact W=8 availability contract where present;
11. exact phase-mate identity and reciprocal pairing;
12. existence of every event-relative coordinate `k=-1..+7` required by
    the frozen endpoint window.

No partial-pass item is allowed.

`REQUIRED_EXACT_ITEM_PASS_COUNT = 336`

`REQUIRED_EXACT_ITEM_FAILURE_COUNT = 0`

Any mismatch produces:

`BLOCKED_ACTIVE_ENCODING_TOKEN_COORDINATE_EQUIVALENCE`

## 8. Active token evidence to freeze

The future equivalence audit must deterministically record the active
encoding used for each item without performing the scientific
matched-versus-swapped interpretation.

At minimum it must record:

- full token-ID sequence or canonical token-ID SHA256 for the prefix;
- full token-ID sequence or canonical token-ID SHA256 for each of the four
  reconstructed branches;
- exact token count for each sequence;
- matched `t_e`;
- swapped `t_e`;
- absolute index corresponding to every `k=-1..+7`;
- exact active token ID at every such coordinate for all four branches.

The output must be canonical and deterministic.

A repeat encoding of the same input under the same authenticated active
encoding must be byte-identical at the token-ID level.

`ACTIVE_ENCODING_REPEAT_IDENTITY_REQUIRED = YES`

The equivalence artifact may preserve the exact active token evidence for
later separately-authorized scientific analysis.

It must not itself classify:

- matched-versus-swapped first difference;
- token-window identity versus difference;
- cross-level token/state relation;
- distinct-to-identity transition.

`SCIENTIFIC_TOKEN_RELATION_CLASSIFICATION_AUTHORIZED = NO`

## 9. Success semantics

A future real audit may return PASS only if all 336 items satisfy every
exact frozen contract requirement.

Permitted success marker:

`PASS_ACTIVE_ENCODING_TOKEN_COORDINATE_EQUIVALENCE`

That PASS means only:

- the active encoding bytes are frozen by the declared manifest;
- frozen P0 branch reconstruction is exact;
- every persisted historical token-coordinate invariant is reproduced
  exactly;
- the active token evidence and event-relative coordinates are deterministic
  and frozen for subsequent analysis.

It does not mean:

- historical tokenizer bytes were recovered;
- historical full branch token sequences were recovered;
- P1 scientific conclusions changed;
- matched/swapped token relation has been scientifically interpreted;
- P2 state evidence has been re-evaluated.

Upon such PASS only:

`NEXT_TOKEN_WINDOW_SCIENCE_PROVENANCE_PREREQUISITE = SATISFIED_BY_ACTIVE_ENCODING_EQUIVALENCE`

If any exact check fails:

`NEXT_TOKEN_WINDOW_SCIENCE_PROVENANCE_PREREQUISITE = BLOCKED`

## 10. Authorized implementation delta

This authority permits creation or modification of exactly:

`scripts/longterm_k0_rvg_post_p2_active_encoding_token_coordinate_equivalence.py`

`tests/test_longterm_k0_rvg_post_p2_active_encoding_token_coordinate_equivalence.py`

No other implementation, report, artifact, or source file is authorized by
this implementation authority.

The implementation should reuse or faithfully preserve the already
validated frozen branch-reconstruction and token-contract semantics where
possible, but must remain provenance-only.

## 11. Synthetic validation requirements

Implementation validation must cover at minimum:

- exact branch reconstruction;
- exact prefix token count match;
- exact prefix token-ID SHA256 match;
- exact four-branch count match;
- exact matched `t_e` match;
- exact swapped `t_e` match;
- exact divergence-offset match where persisted;
- exact W=8 coordinate availability;
- missing-coordinate failure;
- single-token-ID drift failure;
- token-count-preserving token-ID drift behavior;
- anchor-preserving but token-ID-different behavior;
- phase-mate mismatch failure;
- active manifest mismatch rejection before tokenizer load;
- Transformers version mismatch rejection;
- non-fast tokenizer rejection;
- network fallback prohibition;
- deterministic repeat identity;
- canonical output repeat identity;
- duplicate authority-marker rejection;
- real-input read blocked before real execution authority authentication.

Synthetic validation may use fake tokenizers and synthetic inputs only.

## 12. Explicitly not authorized

`REAL_ACTIVE_ENCODING_EQUIVALENCE_AUDIT_AUTHORIZED = NO`

`REAL_P0_SCIENTIFIC_INPUT_READ_AUTHORIZED = NO`

`REAL_P2_ARTIFACT_READ_AUTHORIZED = NO`

`POST_P2_TOKEN_WINDOW_REAL_ARTIFACT_EXECUTION_AUTHORIZED = NO`

`SCIENTIFIC_TOKEN_RELATION_CLASSIFICATION_AUTHORIZED = NO`

`MODEL_CONSTRUCTION_AUTHORIZED = NO`

`CHECKPOINT_LOADING_AUTHORIZED = NO`

`SCIENTIFIC_MODEL_FORWARD_AUTHORIZED = NO`

`SCIENTIFIC_RECURRENT_STATE_READ_AUTHORIZED = NO`

`LOGITS_READ_AUTHORIZED = NO`

`NETWORK_ACCESS_AUTHORIZED = NO`

`SNAPSHOT_DOWNLOAD_AUTHORIZED = NO`

`TRAINING_AUTHORIZED = NO`

`EVALUATION_AUTHORIZED = NO`

`KAGGLE_EXECUTION_AUTHORIZED = NO`

`COMMIT_PUSH_AUTOMATION_AUTHORIZED = NO`

## 13. Next boundary

After implementation and synthetic validation, and only if that validation
passes, a separate minimal one-time read-only execution authority may be
considered for the real 336-item active-encoding equivalence audit.

`NEXT_BOUNDARY = K0_RVG_POST_P2_ACTIVE_ENCODING_TOKEN_COORDINATE_EQUIVALENCE_IMPLEMENTATION_VALIDATION`