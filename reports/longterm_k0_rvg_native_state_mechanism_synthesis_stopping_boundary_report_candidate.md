# ContraMamba K0-RVG Native-State Mechanism Synthesis and Decomposition Stopping Boundary
## Report Candidate

## 1. Status

**Phase:** K0-RVG mechanism synthesis / observational-decomposition stopping boundary.

**Current terminal evidence freeze:**

`848ba9e7c01d37f571f3b3eb2e66ee0497d6f0c3`

**Current terminal validated result:**

`S1/P2` with supporting axis `C2`.

This report does not add new scientific evidence.

It synthesizes already-frozen K0 evidence, defines the strongest supported mechanism statement, identifies unresolved causal claims, and closes further routine observational decomposition.

---

## 2. Purpose

The K0 line has accumulated enough validated localization that continued decomposition now risks becoming post-hoc scalar archaeology rather than progressively stronger mechanism science.

The purpose of this report is therefore to answer four questions:

1. What has actually been established?
2. What is the narrowest supported mechanism chain?
3. What remains observational rather than causal?
4. Where must decomposition stop, and what kind of next experiment would constitute genuine scientific progress?

---

## 3. K0 stopping decision

**Decision: routine observational decomposition stops at `848ba9e`.**

No further K0 stage should be opened merely to correlate the terminal channel mass with another previously frozen scalar, rank, subgroup, top-k list, or descriptive partition.

A new K0 stage after this report must introduce a genuinely new falsifiable claim.

The preferred next class is:

**minimal causal falsification / intervention design**.

That future design is not authorized by this report alone.

---

## 4. Superseded unfrozen candidate

The unfrozen candidate:

`longterm_k0_rvg_frozen_strong_channel_mass_lag0_property_accounting_static_design_candidate.md`

must not be treated as authority.

It was generated after `848ba9e` but intentionally abandoned before freeze because the scientific return from one more cross-stage scalar-accounting step is lower than the risk of post-hoc decomposition.

If a local untracked copy exists, it remains non-authoritative and out of scope.

---

## 5. Scientific scope of the synthesis

This synthesis concerns the frozen K0 native-Mamba recurrence mechanism line at:

- common DDSSSSS cohort;
- current-token divergence-aligned coordinate;
- validated `k=2` mechanism localization;
- layer-22 / layer-21 / block-20 residual-state pathway;
- frozen strong lag-0 kernel partition.

It does not generalize beyond those frozen conditions unless separately validated.

---

## 6. Initial localization: divergence timing

The earlier token/state localization established that the dominant historical 335/336 identity was caused by window/exposure misalignment rather than absence of a real divergence.

Under the corrected equal-length divergence-aligned prefix:

- `k=-1` token/state remained exact-identical;
- `k=0` was the first token difference;
- previous state was still shared at divergence onset;
- gate/write/post-update state diverged immediately;
- differences persisted at later relative coordinates.

This established the correct temporal origin of the native-state comparison.

---

## 7. Native recurrence localization

Subsequent raw-native state analysis localized the corr-specific transient to selective channels rather than a broad global increase.

The relevant energy was associated with channels combining:

- large mean update/input factor;
- substantial role difference in transformed write input.

The mechanism was therefore not a uniform state-scale phenomenon.

---

## 8. Post-update state carrier

Frozen layer-22 readout/state factorization established that the post-update native state term was the dominant state-side carrier of the relevant contrast.

Representative validated population values included:

- state-side corr contrast above ctrl;
- state readout contrast substantially larger for corr than ctrl.

This moved the mechanism from generic layer activation differences to a native recurrent-state carrier.

---

## 9. Carry versus current write

The layer-22 state decomposition:

`S_post = Carry + Write`

showed that the contrast was overwhelmingly current-write dominated.

Frozen evidence showed:

- post-state contrast approximately matched write contrast;
- carry contribution was much smaller;
- write energy accounted for approximately 99% of the relevant state-side contrast.

Therefore the mechanism was not primarily persistence of an already-different previous state.

It was generated at the current update.

---

## 10. Write decomposition

The current write was factorized as:

`W = D * U`.

Validated evidence established:

- U-side magnitude contribution dominated D-side magnitude contribution;
- U–D interaction/cross-term was negative;
- role-dependent cancellation remained substantial.

Thus the write phenotype was:

**U-dominant magnitude with secondary role-dependent U–D cancellation.**

This remained observational/algebraic.

---

## 11. Layer-22 U-path source localization

The next localization followed the U path upstream.

The corr-specific U-path phenotype was already present at the layer-22 mixer-input representation.

It was:

- attenuated by the hidden in-projection;
- selectively amplified by the fixed depthwise convolution;
- only modestly further amplified by SiLU.

This identified the depthwise convolution as an important fixed transfer stage rather than the original source of the entire difference.

---

## 12. Four-tap convolution localization

The fixed depthwise convolution was decomposed by temporal tap.

Validated evidence established:

- lag 0 / current token was the primary positive contribution;
- lag 1 counteracted the effect;
- lag 2 contributed only weakly;
- lag 3 was effectively zero.

Therefore the relevant convolutional transfer was primarily current-token local.

---

## 13. Lag-0 channel-conditioned transfer

For the lag-0 norm-transfer geometry, the exact frozen identity was:

`N² - 1 = Σ_j p_j a_j`

with:

- `p_j` = normalized hidden-difference energy;
- `a_j` = centered squared-kernel strength.

The corr-vs-ctrl difference was:

`Σ_j Δp_j a_j`.

The validated result established that corr placed more normalized current-token hidden-difference energy on above-mean kernel-strength channels.

This was a role-dependent routing/alignment effect against a fixed kernel profile.

---

## 14. Current-token in-projection strong routing

The fixed hidden in-projection row gains differed between strong and weak kernel-linked rows.

However the frozen result showed that the corr-specific effect could not be reduced to a uniform transfer-gain explanation.

Instead the relevant difference was produced by role-dependent alignment of the current-token hidden difference with fixed projection rows.

This reinforced the interpretation of selective routing rather than global amplification.

---

## 15. Layer-22 RMSNorm routing source

RMSNorm decomposition did not yield a single-component explanation.

The frozen result was mixed:

- residual-side contribution mattered;
- scale contribution mattered;
- interaction/cancellation between them was substantial.

Therefore the mechanism could not be compressed into a simple "RMSNorm scale causes routing" statement.

---

## 16. Layer-22 residual construction

At:

`R22 = R21 + Y21`

the frozen result was mixed.

Both incoming residual and layer-21 update contributed, together with a substantial additive interaction.

The layer-22 boundary therefore reflected coherent accumulation rather than one isolated additive source.

---

## 17. Layer-21 residual construction

At:

`R21 = R20 + Y20`

the strong partition produced a qualitatively sharper pattern.

Validated strong-side decomposition showed:

- relatively small direct R20-only contribution;
- relatively small direct Y20-only contribution;
- dominant positive interaction term.

This identified the strong-side `R20 × Y20` interaction as the key construction term.

---

## 18. Strong-side interaction geometry

The strong-side interaction was factorized into:

- R20 magnitude;
- Y20 magnitude;
- directional alignment.

The exact decomposition showed that approximately:

`96.3%`

of the population contrast was attributable to directional alignment.

Magnitude changes were small reinforcing components.

Thus the mechanism at this boundary was:

**frozen-strong-subspace directional-alignment dominance.**

---

## 19. Sign contribution decomposition

The alignment-dominant interaction was further decomposed into:

- positive same-sign co-contribution;
- cancellation relief.

Validated population result:

- positive co-contribution:
  approximately `69.88%`;
- cancellation relief:
  approximately `30.12%`.

Both reinforced the positive interaction contrast.

The primary result was:

**positive co-contribution dominant with secondary cancellation relief.**

---

## 20. Count versus per-channel strength

The same sign-contribution stage showed that the population effect was jointly expressed through:

- participation-count shifts;
- per-channel signed-strength shifts.

For the positive side:

- more same-sign channels contributed;
- positive mass per same-sign channel also increased.

For cancellation relief:

- fewer opposite-sign channels contributed;
- cancellation magnitude per opposite-sign channel also weakened.

Thus neither count nor strength alone fully described the signed interaction.

---

## 21. Itemwise breadth

The population-level result was then tested across all 330 frozen common items.

Validated result:

`A1/B1_BROAD_POSITIVE_TOTAL_WITH_MAJORITY_PARENT_CONCORDANCE`.

Key quantities:

- positive total:
  `295/330 = 89.39%`;
- parent-concordant positive-co-contribution-led:
  `236/330 = 71.52%`;
- positive co-contribution:
  `274/330 = 83.03%`;
- positive cancellation relief:
  `307/330 = 93.03%`;
- both components reinforce:
  `256/330 = 77.58%`;
- positive-component dominant:
  `270/330 = 81.82%`.

Therefore the population mean was not generated by a small minority of extreme items.

The phenotype was broad across examples.

---

## 22. Itemwise count-strength refinement

The same breadth analysis showed a useful asymmetry:

- same-sign participation-count support:
  `200/330 = 60.61%`;
- positive per-channel strength support:
  `277/330 = 83.94%`;
- cancellation-strength relief:
  `313/330 = 94.85%`.

Thus the phenotype was much more consistent in per-channel signed strength than in raw participation count.

The supported description became:

**strength-consistent with only moderate count consistency.**

---

## 23. Channel-mass concentration

The terminal K0 decomposition asked whether the itemwise-broad phenotype was also broadly distributed across strong channels.

Validated result:

`S1/P2`

with supporting:

`C2`.

Key quantities:

- positive channels:
  `122/240`;
- negative channels:
  `118/240`;
- positive-support effective channel count:
  `28.60205938176289`;
- absolute-mass effective channel count:
  `52.50818654659294`;
- positive 50%-mass count:
  `10`;
- positive 80%-mass count:
  `32`;
- absolute 50%-mass count:
  `22`;
- absolute 80%-mass count:
  `75`.

Thus the channel sign field was almost balanced while the channel mass was strongly concentrated.

---

## 24. Joint item-channel structural conclusion

The final frozen K0 structural result is:

**broad across items, concentrated across channels.**

This means:

- the phenotype is not an item-minority artifact;
- the positive net is not a broad sign consensus across strong channels;
- instead a relatively small effective channel set carries most of the interaction magnitude across a broadly expressed item-level phenomenon.

This two-dimensional statement is stronger and more informative than either breadth result alone.

---

## 25. Narrowest supported mechanism chain

The strongest frozen observational mechanism chain is:

**divergence-aligned current token**
→ **layer-22 current update**
→ **write-dominated post-state**
→ **U-dominant write magnitude with U–D cancellation**
→ **current-token / lag-0 selective routing through fixed depthwise structure**
→ **mixed residual construction**
→ **strong-side R20×Y20 interaction**
→ **directional-alignment dominance**
→ **positive co-contribution dominant signed interaction**
→ **broad itemwise expression**
→ **concentrated channel mass**.

Every arrow in this chain should be read as:

**validated localization / decomposition linkage**

unless a separate causal intervention has established more.

---

## 26. What has been established strongly

The following claims are supported by frozen validated evidence:

1. The relevant divergence is real and correctly temporally aligned.
2. The native post-update state is an important carrier.
3. The current write dominates carry at the key layer.
4. U-side write magnitude dominates D-side magnitude.
5. The depthwise-convolution transfer is primarily current-token / lag-0.
6. Corr and ctrl differ in how current-token hidden-difference energy aligns with fixed channel structure.
7. The strong-side layer20→21 residual interaction is alignment dominated.
8. Positive co-contribution is the dominant signed component.
9. The effect is broad across items.
10. The mass is concentrated across channels.

---

## 27. What has not been established

The following remain unproven:

- that any identified channel is causally necessary;
- that any identified channel is causally sufficient;
- that changing lag-0 routing causes the downstream interaction;
- that changing the strong-channel interaction changes the final decision;
- that U-side magnitude is independently causal;
- that the channel concentration itself is mechanistically necessary;
- that frozen kernel strength causes the routing phenotype;
- that the identified chain generalizes to another dataset, seed, architecture, layer, or relative coordinate;
- that the same mechanism controls task accuracy.

These require new evidence classes, not more observational decomposition.

---

## 28. Why routine decomposition stops here

Further routine decomposition would now tend to take one of four forms:

1. correlate terminal channel mass with another already-observed scalar;
2. inspect increasingly small top-k channel subsets;
3. create result-dependent subgroups;
4. search old artifacts for the property with the strongest association.

All four increase post-hoc flexibility without proportionally increasing mechanistic certainty.

Therefore they are outside the default K0 workflow after this report.

---

## 29. Explicit stopping criterion

K0 observational decomposition is considered scientifically saturated when all three conditions hold:

### A. Carrier localization

A concrete native-Mamba carrier has been identified.

**Satisfied.**

### B. Construction localization

The carrier has been localized to a bounded algebraic interaction rather than a generic activation difference.

**Satisfied.**

### C. Population structure

The effect has been characterized across both item and channel dimensions.

**Satisfied.**

Accordingly, the stopping condition is met.

---

## 30. No top-k promotion

The facts that:

- 10 channels capture at least 50% of positive mass;
- 32 channels capture at least 80%;

do not authorize those sets as new scientific populations.

Do not create a "top-10 mechanism" or "top-32 mechanism" without a prospective independent reason.

The concentration result is population-level evidence, not subset authority.

---

## 31. No property fishing

Do not continue by testing a sequence such as:

- lag-0 `mean_delta_p`;
- kernel rank;
- in-projection gain;
- U magnitude;
- another stored channel statistic;

until one appears to explain the terminal mass.

That would transform a mechanistic program into post-hoc predictor selection.

Any future property analysis must originate from a new scientific hypothesis with a falsification condition.

---

## 32. Boundary for a genuine next scientific stage

A new scientific stage is justified only if it changes the evidence type.

The preferred evidence type is:

**minimal intervention / falsification**.

The intervention should test a central link in the frozen chain, not optimize performance and not search for a useful perturbation.

---

## 33. Preferred future falsification target

The most valuable future causal question is not:

> Which individual channel matters most?

It is:

> Is the frozen strong-side directional-alignment structure necessary for the downstream corr-specific native-state contrast?

This targets the most specific, high-attribution interaction discovered by the observational chain.

It directly tests whether the alignment-dominant construction is merely correlated with the phenotype or functionally required for it.

---

## 34. Preferred intervention philosophy

Any future intervention should satisfy:

- one pre-specified mechanism link;
- one bounded perturbation;
- a matched control perturbation;
- no hyperparameter sweep;
- no top-k search;
- no optimization for task performance;
- no learned intervention;
- exact provenance;
- explicit failure criteria.

The goal is falsification, not improvement.

---

## 35. Intervention design is not yet frozen

This synthesis report does not authorize model execution.

It does not authorize GPU use.

It does not authorize a channel mask or activation edit.

A separate prospective intervention design must be frozen first.

---

## 36. K0 versus K1 boundary

The K0 observational line should not be extended automatically into K1.

The existing unrelated K1 files remain out of scope.

A K1 transition requires an explicit new scientific objective and authority.

This report closes routine K0 decomposition but does not itself open K1.

---

## 37. Current scientific status

### Code correctness

Established for the individual frozen stages through their respective gates, static analyzers, and independent validators.

### Execution success

Established for the validated stages.

### Artifact / provenance validity

Established for the frozen evidence chain used here.

### Scientific conclusion

The K0 line supports a coherent observational native-state mechanism localization with a clear stopping boundary.

It does not yet establish causal necessity or sufficiency.

---

## 38. Terminal K0 evidence

Current terminal evidence freeze:

`848ba9e7c01d37f571f3b3eb2e66ee0497d6f0c3`

Commit message:

`Freeze strong channel-mass breadth concentration evidence`

Terminal validated classification:

`S1/P2` with `C2`.

This commit is the endpoint for routine K0 decomposition unless superseded by a newly frozen scientific authority.

---

## 39. Synthesis conclusion

The accumulated frozen evidence supports the following compact scientific statement:

**At the validated divergence-aligned current token, the ContraMamba K0 phenotype is carried primarily by the native current write into layer-22 post-update state. Upstream construction localizes to selective current-token routing through fixed channel structure and, at the layer20→21 strong subspace, to a directional-alignment-dominant interaction whose signed advantage is primarily positive co-contribution with secondary cancellation relief. The phenotype is broadly expressed across items but strongly concentrated in channel mass.**

This is the strongest supported observational statement.

---

## 40. Stopping-boundary conclusion

**Routine observational decomposition stops here.**

A future stage must not justify itself merely by being "one level deeper."

It must answer a different class of question:

**Does a pre-specified perturbation falsify or support a central functional link in the frozen mechanism chain?**

That is the required standard for further K0 scientific progress.

---

## 41. Next authorized workflow after this report is frozen

After this synthesis/stopping-boundary report is frozen, the controller should open a new prospective scientific design only for:

**minimal causal falsification of the strong-side directional-alignment mechanism.**

Before any implementation or execution, that design must specify:

- exact intervention object;
- exact preserved controls;
- exact expected directional consequence;
- exact falsification outcome;
- exact scope and run budget;
- whether model execution is required;
- provenance requirements.

No training or evaluation is authorized by default.

---

## 42. Final status

**K0 observational decomposition:** READY TO CLOSE.

**Mechanism synthesis:** READY TO FREEZE.

**Further scalar decomposition:** NOT RECOMMENDED / NOT DEFAULT-AUTHORIZED.

**Next scientific evidence class:** prospective minimal causal falsification.

**GPU / Kaggle:** NOT NEEDED for this synthesis stage.

**K1:** NOT OPENED.
