# K0-RVG-P2 Validated Scientific Interpretation Report Candidate

**Status:** validated scientific interpretation candidate.

**Date:** 2026-09-12

**Frozen P2 one-time execution authority commit:**

`08f2cbcec6fac26cc493a4aa21559718c9a9e4af`

**Frozen P2 implementation commit:**

`8e366761a92392ac0adc3c6550bccdaf791f14cf`

**Validated P2 output artifact:**

`C:\Users\Home1\Desktop\ContraMamba-K0-RVG-P2-Runs\p2-static-diagnostic-88d021a-v1\p2_degeneracy_diagnostic.json`

**Validated P2 output SHA256:**

`058d00adb99cdbfad1893593a3e8917322c5ca2f186f7305561460f868ddddc3`

This report interprets only the already executed and provenance-validated K0-RVG-P2 static diagnostic.

It does not authorize a rerun, new model execution, recurrent-state read, training, causal intervention, learned geometry, subgroup search, or K4.

The historical K-series A/B fork remains context only and is not reopened.

## 1. Evidence-layer separation

The following four layers remain distinct.

### 1.1 Code correctness

The frozen implementation-readiness stage established:

`P2_CODE_CORRECTNESS = PASS_FOR_FROZEN_STATIC_DIAGNOSTIC_CONTRACT`

`P2_FOCUSED_TEST_CONTRACT = PASS_18`

`P2_SYNTHETIC_IMPLEMENTATION_VALIDATION = PASS`

### 1.2 Execution success

The one authorized real-artifact static diagnostic completed once with:

`status = COMPLETE_P2_STATIC_DEGENERACY_DIAGNOSTIC`

and reported:

`p1_scientific_rerun_executed = false`

`scientific_model_forward_executed = false`

`scientific_recurrent_state_read = false`

### 1.3 Artifact / provenance validity

The produced artifact set contained exactly one file:

`p2_degeneracy_diagnostic.json`

Size:

`405619 bytes`

SHA256:

`058d00adb99cdbfad1893593a3e8917322c5ca2f186f7305561460f868ddddc3`

Post-execution read-only validation established:

`PASS_CANONICAL_JSON`

`PASS_SCHEMA_AND_ITEM_DOMAIN`

`PASS_72_HASH_CONTRACT_ALL_336`

`PASS_SUMMARY_COUNT_CONSISTENCY`

`PASS_FROZEN_PROVENANCE_BINDINGS`

`PASS_P2_ARTIFACT_AND_PROVENANCE_VALID`

### 1.4 Scientific interpretation

Only after the above validation is the P2 output treated as scientific evidence.

## 2. Frozen P1 fact being localized

The frozen corrected P1 result remains:

`RAW_NATIVE_VECTOR_ORGANIZATION_NOT_ESTABLISHED`

P1 had:

`turning_exact_zero_count = 335 / 336`

`coherence_exact_zero_count = 335 / 336`

P2 does not rescue or replace that negative result.

P2 asks where those 335 exact-zero matched-versus-swapped contrasts arise in the already persisted evidence chain.

## 3. Primary P2 result

The strongest validated P2 result is:

`335 / 336` items are simultaneously classified as:

- `TURNING_COMPONENTWISE_IDENTITY`;
- `COHERENCE_EXACT_IDENTITY`;
- `ENDPOINT_SUPPORTING_STATE_HASH_IDENTITY`;
- exact equality on all six persisted response/carry/write diagnostic summaries.

For those same `335 / 336` items, both primary exact-zero endpoints localize to:

`STATE_IDENTITY_EXPLAINS_ENDPOINT_IDENTITY`

Therefore the dominant P1 degeneracy is not explained by a turning-endpoint subtraction cancellation.

Instead, for 335 items, the matched/swapped branches are already indistinguishable under every persisted endpoint-supporting SHA256 comparison used by P2.

`P2_PRIMARY_RESULT = ENDPOINT_SUPPORTING_STATE_IDENTITY_LOCALIZES_335_OF_336_P1_ZERO_CONTRAST_ITEMS`

## 4. Turning endpoint

Validated classification counts:

`TURNING_COMPONENTWISE_IDENTITY = 335`

`TURNING_DIFFERENT = 1`

There are:

`TURNING_EQUAL_BY_OFFSET_CANCELLATION = 0`

within the validated artifact.

This matters because the dominant exact-zero turning contrast is not produced by different matched/swapped turning components canceling after subtraction.

For 335 items, the persisted matched/swapped turning components themselves are exactly equal.

Validated localization counts:

`STATE_IDENTITY_EXPLAINS_ENDPOINT_IDENTITY = 335`

`None = 1`

Thus:

`P2_TURNING_ZERO_CONTRAST_CANCELLATION_EXPLANATION = REJECTED_FOR_ALL_335_ZERO_ITEMS`

`P2_TURNING_ZERO_CONTRAST_STATE_IDENTITY_LOCALIZATION = 335_OF_335_ZERO_ITEMS`

## 5. Response-coherence endpoint

Validated classification counts:

`COHERENCE_EXACT_IDENTITY = 335`

`COHERENCE_DIFFERENT = 1`

Validated localization counts:

`STATE_IDENTITY_EXPLAINS_ENDPOINT_IDENTITY = 335`

`None = 1`

The frozen P1 response-coherence degeneracy is therefore likewise localized upstream of the final contrast computation for all 335 exact-zero items.

P2 does not infer unpersisted per-tau coherence terms.

It establishes only exact identity of the persisted coherence summary together with endpoint-supporting state-hash identity.

`P2_COHERENCE_ZERO_CONTRAST_STATE_IDENTITY_LOCALIZATION = 335_OF_335_ZERO_ITEMS`

## 6. Endpoint-supporting state-hash evidence

For each item, P2 compared exactly:

`2 branch-role pairs * 9 event-relative coordinates * 4 tensor fields = 72`

persisted SHA256 values.

The branch-role pairs were:

`matched_corr <-> swapped_corr`

`matched_ctrl <-> swapped_ctrl`

The event-relative coordinates were:

`-1, 0, 1, 2, 3, 4, 5, 6, 7`

The fields were:

`S_prev_sha256`

`G_sha256`

`W_sha256`

`S_post_sha256`

Validated counts:

`ENDPOINT_SUPPORTING_STATE_HASH_IDENTITY = 335`

`ENDPOINT_SUPPORTING_STATE_HASH_DIFFERENCE = 1`

For each of the 335 identity items:

`72 / 72` comparisons are exact SHA256 identity under the frozen P2 comparison contract.

This is byte-identity evidence for the persisted endpoint-supporting tensors represented by those hashes.

It is not a numerical-distance claim.

It is not a claim that all hidden states everywhere in the model or at all earlier positions are identical.

It is not a causal explanation for why the identity occurs.

## 7. The unique common exception

The read-only cross-localization audit established:

`all_exception_sets_identical = true`

Every non-identity marginal set points to the same single item:

`local_template_index = 163`

`stable_item_id = k0-rvg-p0-v1:770595a19a44b178faa6d32dae44b7bb0833a744dfecd4e27e2239e7c4c152a2`

This item is the sole exception simultaneously for:

- turning classification;
- coherence classification;
- endpoint-supporting state-hash classification;
- `response_norm_mean`;
- `carry_response_norm_mean`;
- `write_response_norm_mean`;
- `write_total_cosine_mean`;
- `carry_total_cosine_mean`;
- `write_carry_cosine_mean`.

Therefore the `335 / 336` pattern is not formed by different exceptional items in different diagnostic families.

It is a single common exception.

`P2_COMMON_EXCEPTION_LOCAL_TEMPLATE_INDEX = 163`

`P2_COMMON_EXCEPTION_ALL_DIFFERENCE_SETS_IDENTICAL = YES`

## 8. State-hash structure of the common exception

For item 163:

`ENDPOINT_SUPPORTING_STATE_HASH_DIFFERENCE`

with:

`exact_equal_count = 61`

`different_count = 11`

out of:

`72`

The first persisted difference in the deterministic P2 comparison order is:

`branch_role = ctrl`

`relative_coordinate = +5`

`tensor_field = G_sha256`

with:

`matched_token_index = 48`

`swapped_token_index = 48`

This establishes only the first difference under the frozen comparison order.

It does not establish that this `G` difference is the causal origin of the later endpoint difference.

The validated P2 artifact does not preserve the full list of all 11 differing hash coordinates, so this report does not infer their detailed pattern.

`P2_EXCEPTION_STATE_HASH_EXACT_EQUAL_COUNT = 61`

`P2_EXCEPTION_STATE_HASH_DIFFERENT_COUNT = 11`

`P2_EXCEPTION_FIRST_DIFFERENCE_BRANCH_ROLE = ctrl`

`P2_EXCEPTION_FIRST_DIFFERENCE_RELATIVE_COORDINATE = 5`

`P2_EXCEPTION_FIRST_DIFFERENCE_TENSOR_FIELD = G_sha256`

## 9. Persisted response/carry/write diagnostics

Each of the six persisted diagnostic summaries has the same count:

`EXACT_EQUAL = 335`

`DIFFERENT = 1`

The one different item is always item 163.

Therefore the dominant 335-item identity is not confined to the two primary endpoints.

It is also present in every persisted P2-authorized response/carry/write summary.

This broadens the localization of the degeneracy within the frozen evidence.

It does not promote these summaries to primary endpoints or causal mechanisms.

## 10. Frozen construction evidence

Validated construction relations are:

### 10.1 Divergence anchor

`divergence_anchor_exact_equal = TRUE` for `336 / 336`.

### 10.2 W=8 availability

`w8_availability_exact_equal = TRUE` for `336 / 336`.

### 10.3 Correction token count

`correction_token_count_exact_equal = TRUE` for `330 / 336`.

`correction_token_count_exact_equal = FALSE` for `6 / 336`.

### 10.4 Control token count

`control_token_count_exact_equal = TRUE` for `330 / 336`.

`control_token_count_exact_equal = FALSE` for `6 / 336`.

The six correction-count mismatch items and six control-count mismatch items are exactly the same local indices:

`63, 95, 163, 231, 263, 331`

### 10.5 Frozen continuation text

`correction_continuation_text_relation = DIFFERENT` for `336 / 336`.

`control_continuation_text_relation = DIFFERENT` for `336 / 336`.

Thus surface continuation-text difference is present in every item, including the 335 state-hash-identity items.

Within this frozen dataset, different continuation text is therefore not sufficient to produce a difference in the endpoint-supporting persisted state hashes.

## 11. Token-count mismatch is not sufficient for state-hash difference

Six items have unequal matched/swapped correction and control token counts:

`63, 95, 163, 231, 263, 331`

Only item:

`163`

has endpoint-supporting state-hash difference.

The other five token-count mismatch items remain in:

`ENDPOINT_SUPPORTING_STATE_HASH_IDENTITY`

Therefore:

`TOKEN_COUNT_MISMATCH -> STATE_HASH_DIFFERENCE`

is false as a deterministic explanation within this dataset.

This does not establish the converse.

It does not prove token-count mismatch is irrelevant.

It establishes only that token-count mismatch alone is insufficient to account for the P2 state-hash difference.

`P2_TOKEN_COUNT_MISMATCH_ITEMS = 6`

`P2_TOKEN_COUNT_MISMATCH_WITH_STATE_IDENTITY_ITEMS = 5`

`P2_TOKEN_COUNT_MISMATCH_WITH_STATE_DIFFERENCE_ITEMS = 1`

## 12. Strongest scientifically supported interpretation

The strongest supported interpretation is:

For the 335 P1 exact-zero items, the matched/swapped branches do not merely converge to the same scalar endpoint after different internal endpoint-supporting states.

Under the frozen P2 observation window and persisted tensor-hash fields, the branches are already exactly identical at the endpoint-supporting state level.

This supports a localization claim:

`the dominant P1 degeneracy lies upstream of the endpoint metrics`

rather than:

`the endpoint metrics erase otherwise different endpoint-supporting states`.

More precisely:

`P2_DOMINANT_DEGENERACY_LOCALIZATION = UPSTREAM_OF_PERSISTED_ENDPOINT_SUPPORTING_STATE_WINDOW_OR_AT_ITS_INPUT_BOUNDARY`

The exact causal origin remains unresolved.

## 13. What P2 does not establish

P2 does not establish any of the following:

- why the 335 branches become state-hash identical;
- the earliest token position at which identity begins;
- whether matched/swapped token-ID sequences are identical or different;
- whether tokenization aliases distinct surface text;
- whether differences exist before event-relative coordinate `-1`;
- whether differences occur in model quantities not covered by the four persisted hash fields;
- whether any particular gate, write, carry, or recurrence component causally removes a difference;
- whether item 163 is representative of a scientifically meaningful subgroup;
- whether a new geometry would recover a positive effect;
- whether changing W, layer, construction, or endpoint would alter the result.

These require a separately specified next scientific question and new authority if pursued.

## 14. Consequence for the original native-vector hypothesis

The P1 negative result remains valid.

P2 does not provide evidence for raw native vector organization.

Instead, P2 explains why the primary matched/swapped contrast had almost no effective variation:

for 335 items, the persisted endpoint-supporting native-state evidence itself is exactly identical across matched/swapped branches.

Therefore no downstream endpoint built deterministically from those same persisted endpoint-supporting quantities can recover a matched/swapped distinction for those items without introducing new information or a different observation boundary.

This is a limitation/localization result, not a positive geometry result.

`P2_RAW_NATIVE_VECTOR_ORGANIZATION_ESTABLISHED = NO`

`P1_NEGATIVE_RESULT_RESCINDED = NO`

## 15. Next scientific question

The next scientific question should not be a hyperparameter sweep and should not immediately introduce a more complex geometry.

The narrow next question is:

> At what earliest authorized state-blind or already-persisted boundary do the matched and swapped constructions remain observably distinct before the 335-item endpoint-supporting state identity appears?

The first priority is to exhaust frozen/state-blind provenance and already persisted evidence before authorizing any new model forward or recurrent-state capture.

A future stage may investigate whether the identity is already implied by token-level construction/provisioning evidence or whether a new, narrowly justified observation boundary is necessary.

No such execution is authorized by this report.

## 16. Final validated interpretation markers

`HISTORICAL_AB_FORK_AUTHORITY = CONTEXT_ONLY_SUPERSEDED_BY_K0_RVG`

`P2_EXECUTION_AUTHORITY_COMMIT = 08f2cbcec6fac26cc493a4aa21559718c9a9e4af`

`P2_OUTPUT_SHA256 = 058d00adb99cdbfad1893593a3e8917322c5ca2f186f7305561460f868ddddc3`

`P2_ARTIFACT_AND_PROVENANCE_VALID = YES`

`P2_ITEM_COUNT = 336`

`P2_ZERO_CONTRAST_ITEM_COUNT = 335`

`P2_ENDPOINT_SUPPORTING_STATE_HASH_IDENTITY_COUNT = 335`

`P2_ENDPOINT_SUPPORTING_STATE_HASH_DIFFERENCE_COUNT = 1`

`P2_TURNING_COMPONENTWISE_IDENTITY_COUNT = 335`

`P2_TURNING_OFFSET_CANCELLATION_COUNT = 0`

`P2_COHERENCE_EXACT_IDENTITY_COUNT = 335`

`P2_COMMON_EXCEPTION_LOCAL_TEMPLATE_INDEX = 163`

`P2_COMMON_EXCEPTION_ALL_DIFFERENCE_SETS_IDENTICAL = YES`

`P2_EXCEPTION_STATE_HASH_EXACT_EQUAL_COUNT = 61`

`P2_EXCEPTION_STATE_HASH_DIFFERENT_COUNT = 11`

`P2_EXCEPTION_FIRST_DIFFERENCE_BRANCH_ROLE = ctrl`

`P2_EXCEPTION_FIRST_DIFFERENCE_RELATIVE_COORDINATE = 5`

`P2_EXCEPTION_FIRST_DIFFERENCE_TENSOR_FIELD = G_sha256`

`P2_CORRECTION_TEXT_DIFFERENT_COUNT = 336`

`P2_CONTROL_TEXT_DIFFERENT_COUNT = 336`

`P2_TOKEN_COUNT_MISMATCH_ITEM_COUNT = 6`

`P2_TOKEN_COUNT_MISMATCH_WITH_STATE_IDENTITY_COUNT = 5`

`P2_PRIMARY_RESULT = ENDPOINT_SUPPORTING_STATE_IDENTITY_LOCALIZES_335_OF_336_P1_ZERO_CONTRAST_ITEMS`

`P2_DOMINANT_DEGENERACY_LOCALIZATION = UPSTREAM_OF_PERSISTED_ENDPOINT_SUPPORTING_STATE_WINDOW_OR_AT_ITS_INPUT_BOUNDARY`

`P2_RAW_NATIVE_VECTOR_ORGANIZATION_ESTABLISHED = NO`

`P1_NEGATIVE_RESULT_RESCINDED = NO`

`P2_REAL_ARTIFACT_DIAGNOSTIC_EXECUTION_AUTHORIZED = CONSUMED`

`P2_REAL_ARTIFACT_DIAGNOSTIC_RERUN_AUTHORIZED = NO`

`SCIENTIFIC_MODEL_FORWARD_AUTHORIZED = NO`

`SCIENTIFIC_RECURRENT_STATE_READ_AUTHORIZED = NO`

`TRAINING_AUTHORIZED = NO`

`CAUSAL_INTERVENTION_AUTHORIZED = NO`

`LEARNED_OR_TUNED_GEOMETRY_AUTHORIZED = NO`

`KAGGLE_EXECUTION_AUTHORIZED = NO`

`K4_EXECUTION_AUTHORIZED = NO`

`NEXT_BOUNDARY = K0_RVG_POST_P2_EARLIEST_DISTINCT_BOUNDARY_HYPOTHESIS_SPECIFICATION`

This report becomes the frozen validated P2 scientific interpretation only after this exact document is committed and pushed as the immediate one-file child of:

`08f2cbcec6fac26cc493a4aa21559718c9a9e4af`
