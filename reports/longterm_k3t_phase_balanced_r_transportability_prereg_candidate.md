# K3T Phase-Balanced R Transportability Preregistration Candidate

**Status:** prospective confirmatory scientific/design preregistration candidate.

**Stage name:** `K3T — Phase-Balanced R Transportability`.

**Frozen design verdict:** `PASS_READY_TO_PREREGISTER_K3T`.

Once committed, this document is intended to be the complete scientific/design authority for bounded K3T implementation and non-scientific validation.

It does **not** itself authorize K3T scientific recurrent-state execution.

K3T is a new observational successor study.

K3T is not a K3C rerun.

K3T is not a K3C rescue.

K3T does not reopen K3.

K3T does not authorize K4.

## 1. Scientific motivation

K2R prospectively replicated the four-endpoint local-versus-net trajectory dissociation on a claim-disjoint 300-item population.

At layer 23, K2R R mean-speed pair-specificity was significantly positive:

- positive blocks = 84;
- negative blocks = 53;
- zero blocks = 13;
- n_eff = 137;
- rank-biserial sign effect = `+0.22627737226277372`;
- Holm-adjusted p = `0.02022834` in the K2R four-endpoint family.

K3C then used a second claim-disjoint 300-item population.

Its mandatory BASE replication gate reproduced D, DISP, and P, but R did not reproduce:

- positive blocks = 78;
- negative blocks = 68;
- zero blocks = 4;
- n_eff = 146;
- rank-biserial sign effect = `+0.0684931506849315`;
- Holm-adjusted p = `0.45648223496512574`;
- direction match = NO;
- directional contradiction = NO.

Therefore K3C closed with:

`INCONCLUSIVE_DUE_TO_BASELINE_REPLICATION_FAILURE`

The subsequent frozen read-only audit established:

`R_CROSS_CLAIM_TRANSPORTABILITY_NOT_ESTABLISHED`

The audit found that the K2R-to-K3C R weakening was not sufficiently explained by:

- correction-source marginal composition;
- matched or swapped d-p marginal composition;
- reciprocal-block d-pattern composition;
- exact reciprocal-block prefix endpoint `(p_a, p_b)` composition.

The audit also established a stop condition against further outcome-guided lexical slicing.

The remaining scientifically identified next question is therefore prospective:

**Does positive R pair-specific speed geometry replicate on a new claim-disjoint population whose deterministic nonnumeric lexical generator phase is exactly balanced?**

K3T answers only that question.

## 2. Governing evidence chain

Relevant frozen anchors are:

K0 native-state kinematics hypothesis:

`7bd1cf824cd53c7f6cf6215346b42cabf351b70a`

K2R preregistration:

`54b1a9a2188e3e678e8378b43faa5d527aca1457`

K2R validated closure/archive:

`ba0ad9052a3b8a5eb80fef46dea461f372f95ee8`

K3 causal-specialization contradiction closure/archive:

`99988e8d2a4c47d6bcaec1bf487aac777076fe2f`

K3C preregistration:

`b85272d88d0bb57db45fdc963d313714529e7975`

K3C scientific closure/archive:

`04b089e5424e1debe669b3ea809d7ab5c4efd111`

K3C R transportability audit:

`aeb2960cd6cbe7ab78c4c0e42e350a6699070d79`

Frozen K3C R transportability audit report:

`reports/longterm_k3c_r_cross_population_transportability_audit_report_candidate.md`

SHA256:

`cf0b7f933d2f3299b7d3e4650337a8950dfa82ef95734e0978b9a2385b61f437`

K3T must preserve all prior negative and inconclusive results.

No K3T outcome may retroactively convert K3C into a successful mechanism experiment.

## 3. Scientific scope

K3T is a:

`SAME_GENERATOR_CLAIM_DISJOINT_PHASE_BALANCED_PROSPECTIVE_R_REPLICATION`

It is observational.

It is not a causal intervention study.

It is not an external-distribution replication.

It uses the same frozen deterministic controlled generator family as K2R and K3C.

Therefore even a successful K3T establishes only a bounded positive R replication under the prospectively phase-balanced controlled-generator construction.

It does not establish:

- natural-corpus generalization;
- unrestricted generator-family transportability;
- final task-decision causality;
- authorization/entitlement causality;
- a W/G/H mechanism;
- detector utility;
- K4 claims.

## 4. Frozen generator identity

Generator path:

`scripts/build_controlled_v5.py`

Generator SHA256:

`4e9798591fbfffb6d15ea9b2f8cf5cd804a9e76713b9f6b13b354fe6ce93aa5c`

Generator Git blob:

`baee23a9f71333125f4a8735c2c92d20cab7eb4f`

No generator modification is permitted for K3T population construction.

The implementation must revalidate the exact file SHA and Git blob before scientific population materialization.

## 5. Frozen phase definition

The generator contains 30 fixed seed templates before the generated deterministic template sequence.

For global template index `g >= 30`, define:

`generated_offset = g - 30`

Define the frozen nonnumeric lexical phase class:

`phase(g) = generated_offset mod 168`

The value 168 is the least common multiple of the frozen generated-factor cardinalities relevant to the nonnumeric lexical template:

- name = 12;
- title = 6;
- role = 6;
- predicate pair = 7;
- time = 6;
- location = 8.

Thus:

`LCM(12, 6, 7, 8) = 168`

The state-blind prospective audit additionally verified phase identity directly using the exact lexical fields:

- `title`;
- `name`;
- `alternate_title`;
- `alternate_name`;
- `role`;
- `alternate_role`;
- `predicate`;
- `alternate_predicate`;
- `time`;
- `alternate_time`;
- `location`;
- `alternate_location`.

The unique generated object-number suffix is not part of the 168-class nonnumeric phase definition.

Exact token sequences are therefore not assumed to repeat every 168 items.

## 6. Frozen K3T global population slice

Generate:

`fact_templates_for_count(1236)`

Then take exactly the Python slice:

`[900:1236]`

This corresponds to exactly 336 global template indices:

900 through 1235 inclusive.

The exact pair-ID range is:

`generated_fact_901`

through:

`generated_fact_1236`

with no gaps, duplicates, replacements, filtering, or search.

K3T population size:

`N_ITEMS = 336`

Reciprocal block count:

`N_BLOCKS = 168`

Because the slice length is exactly:

`2 * 168 = 336`

every frozen nonnumeric lexical phase class must occur exactly twice.

The state-blind prospective audit established:

`PHASE_CLASS_COUNT = 168`

and:

`PHASE_COUNT_UNIQUE_VALUES = [2]`

Any phase-count mismatch is:

`K3T_PHASE_BALANCE_FAILURE`

and stops the study before model execution.

## 7. Frozen phase-by-correction-source balance

The selected 336 templates are passed together as one standalone population to the frozen generator record-construction semantics.

The standalone generated population has exactly:

- `polarity_flip = 168`;
- `none = 168`

as the deterministic REFUTE correction-source counts.

The state-blind prospective audit further established the stronger condition:

**every one of the 168 nonnumeric lexical phase classes contains exactly one `polarity_flip` correction item and exactly one `none` correction item.**

Required marker:

`PHASE_SOURCE_BALANCE=EACH_PHASE_HAS_ONE_POLARITY_FLIP_AND_ONE_NONE`

Any violation is:

`K3T_PHASE_SOURCE_BALANCE_FAILURE`

and stops before model execution.

This phase/source balance is construction control only.

It is not a post-outcome stratum-selection rule.

## 8. Frozen generated-source identity

Expected generated source row count:

`4368`

Canonical generated-source SHA256:

`fb699bcc99e00b8c437fd49c204345933d61c69215af853746bc4639545c7400`

Canonical encoding is:

- UTF-8;
- sorted JSON keys;
- compact separators;
- `ensure_ascii = false`;
- `allow_nan = false`;
- exactly one LF-terminated JSON object per row.

Any mismatch is:

`K3T_GENERATED_SOURCE_IDENTITY_FAILURE`

and stops before model execution.

## 9. Frozen T / E / Q extraction

For each of the 336 generated pairs define:

T:

exactly one `evidence_truncation` row.

E:

exactly one `entity_swap` row.

Q:

the unique REFUTE correction row chosen by the deterministic rule:

1. if exactly one `polarity_flip` row has final label REFUTE, use it;
2. otherwise, if exactly one `none` row has final label REFUTE, use it;
3. otherwise fail closed.

Require for every pair:

T:

- final label = NOT_ENTITLED;
- primary failure type = sufficiency;
- sufficiency label = 0.

E:

- final label = NOT_ENTITLED;
- primary failure type = frame;
- polarity label = NONE.

Q:

- final label = REFUTE;
- polarity label = REFUTE.

Require exact claim identity across T, E, and Q.

No alternate row selection is permitted.

## 10. Frozen textual construction

For recipient i:

`P_i = "Claim: " + T.claim + "\nEvidence: " + T.evidence + "\nAdditional evidence:\n"`

`C_i = Q.evidence`

`N_i = E.evidence`

No text rewriting is permitted.

Forbidden operations include:

- Unicode normalization;
- whitespace repair;
- paraphrasing;
- alternate templates;
- added EOS;
- branch padding;
- truncation;
- continuation-length equalization.

## 11. Frozen K3T candidate identity

For every K3T item construct the canonical recipe with exactly the prospective state-blind fields:

- `schema_version = "k3t-phase-balanced-population-v1"`;
- `generator_sha256`;
- `global_template_start = 900`;
- `global_template_stop = 1236`;
- `global_template_index`;
- `generator_phase_period = 168`;
- `generator_phase_class`;
- `cycle_in_slice`;
- `pair_id`;
- `truncation_source_id`;
- `correction_source_id`;
- `correction_source_intervention`;
- `control_source_id`;
- `prefix_text`;
- `correction_text`;
- `control_text`.

Canonical recipe JSON uses:

- UTF-8;
- sorted keys;
- compact separators;
- `ensure_ascii = false`;
- `allow_nan = false`.

Define:

`stable_item_id = "k3t-v1:" + SHA256(canonical_recipe_json)`

Define:

`base_claim_sha256 = SHA256(exact claim UTF-8 bytes)`

Sort all 336 candidate rows lexicographically by `stable_item_id`.

The exact canonical JSONL SHA256 of the sorted K3T candidate population is:

`d95d245e358ff497ea50b95e4f54d1192be64d09fec2c06538fc15f75b09ef70`

Any mismatch is:

`K3T_CANDIDATE_IDENTITY_FAILURE`

and stops before model execution.

## 12. Frozen claim-disjointness

K3T must be disjoint from all prior native-state scientific populations used in the current evidence chain.

Frozen comparison pools:

K2W:

`reports/longterm_k2w_fixed_window_phase_a_c7c7a0c218bb/candidate_pool.jsonl`

SHA256:

`abf693d3267cc4e3dd27a8127d2948b36fdf8ba24e135a643215f0f31a26d808`

K2R/K3:

`reports/longterm_k2r_claim_disjoint_replication_52bd363_v1/candidate_pool.jsonl`

SHA256:

`00bbdc9977679aa0561be413e7cef8e710dc7987618fe9593e8bf0852a5a7de4`

K3C:

`reports/longterm_k3c_contribution_db75edfbf34b_v1/candidate_pool.jsonl`

SHA256:

`9603f6b20ba870807c151bb70df4c42b0957ded578729b133a37fee8aa1da83e`

The state-blind prospective audit established zero overlap for each prior pool on:

- pair ID;
- exact claim SHA;
- exact claim text.

Required results:

`K2W_OVERLAP={"claim_sha":0,"claim_text":0,"pair_id":0}`

`K2R_K3_OVERLAP={"claim_sha":0,"claim_text":0,"pair_id":0}`

`K3C_OVERLAP={"claim_sha":0,"claim_text":0,"pair_id":0}`

Scientific execution must revalidate all three overlap families as zero before recurrent-state read.

Any nonzero overlap stops execution.

## 13. Frozen reciprocal block assignment

After sorting K3T items by ascending `stable_item_id`, index:

`i = 0,...,335`

Define:

`pi(i) = i XOR 1`

giving exactly 168 reciprocal blocks:

- 0 <-> 1;
- 2 <-> 3;
- ...;
- 334 <-> 335.

For recipient i and donor `j = pi(i)` define:

Matched correction:

`M_corr(i) = P_i + C_i`

Matched control:

`M_ctrl(i) = P_i + N_i`

Swapped correction:

`S_corr(i) = P_i + C_j`

Swapped control:

`S_ctrl(i) = P_i + N_j`

The complete population must preserve exactly the correction and control continuation multisets between matched and swapped assignment.

No alternate pairing is permitted.

The exact canonical reciprocal-mapping SHA256 is:

`1c458054540ac0d38857cb3f55595e0b286428225ef11ed22059981dfd1e28ad`

Any mismatch stops execution.

## 14. Frozen tokenizer and HF identity

Model/tokenizer family:

`state-spaces/mamba-130m-hf`

Exact Hugging Face revision:

`5708daa364c50b880e7bd92eab456e0d34492ee9`

Required Transformers version:

`5.12.1`

Required tokenizer class:

`GPTNeoXTokenizer`

Required:

`tokenizer_is_fast = true`

Tokenizer requirements:

- `trust_remote_code = false`;
- `add_special_tokens = false`;
- no padding;
- no truncation.

Resolved tokenizer/config file identities and SHA256 values must be recorded in the scientific manifest.

## 15. Frozen tokenizer feasibility

The state-blind prospective audit established:

`N_total = 336`

`N_blocks = 168`

`N_matched_valid = 336`

`N_swapped_valid = 336`

Matched first correction/control divergence relative to p:

- `d-p = 2` for 168 items;
- `d-p = 3` for 168 items.

Swapped first correction/control divergence relative to p:

- `d-p = 2` for 168 items;
- `d-p = 3` for 168 items.

Matched correction post-prefix availability:

`[23, 28]`

Matched control post-prefix availability:

`[20, 27]`

Swapped correction post-prefix availability:

`[23, 28]`

Swapped control post-prefix availability:

`[20, 27]`

Required marginal-preservation checks:

- prefix marginal preserved = true;
- correction marginal preserved = true;
- control marginal preserved = true.

These are construction feasibility facts only.

They are not scientific endpoints.

Any scientific-run mismatch against the frozen feasibility contract stops execution.

## 16. Event anchor and fixed window

For each recipient i:

`p_i = len(tokens(P_i)) - 1`

The primary event anchor is the final prefix token.

Primary fixed post-prefix window:

`W = 8`

K3T uses native recurrent states:

`S_(p_i-1), S_(p_i), S_(p_i+1), ..., S_(p_i+8)`

for the layer-23 R calculation.

All four branches for a recipient must have the exact same prefix token sequence through p_i.

All four branches must contain at least eight post-prefix tokens.

No alternate W is permitted.

No onset-aligned replacement window is permitted.

## 17. Frozen checkpoint identity

Use the same authenticated seed180 A0 encoder realization as K2R and K3C.

Handoff ZIP:

`C:\Users\Home1\Downloads\p3w7-seed8192-a0-seed180-replacement-r1-retry1_55debe94f0d1.zip`

Expected ZIP SHA256:

`96859bad3e400613b4c981990e56aaf35b1d92baaaa930eb11448cf38a63b861`

Expected selected checkpoint SHA256:

`4f7ad019bddb988a534c477b58b36bdabe2775d6c9748331e8311653c07c864c`

Expected encoder canonical digest:

`48a7e9ac9dfa6c8c292090ee0fcb606bd4c85d13706bfc8a3e371af77c440597`

Expected encoder raw-concatenation digest:

`968c12c095a6aab883db5984f4c02ad5e893a5ff140781ffbda41b97970401ae`

Expected encoder structure:

- tensor count = 242;
- total numel = 129135360;
- total raw bytes = 516541440;
- dtype = float32.

No training or fine-tuning is authorized.

A0 task-head predictions are forbidden as population-selection, weighting, filtering, or promotion criteria.

## 18. Frozen native-state source

Use direct native selective-SSM recurrent state under:

`MambaMixer.slow_forward`

Required installed source SHA256:

`23c7b410e204b5da01732566de10c94b70a8418ecb608e409754b00332eb2a41`

Required Transformers version:

`5.12.1`

Required scientific device:

`cpu`

Required state timing:

`post_consumption_s_t`

Required recurrence source semantics:

`S_t = discrete_A_t ⊙ S_(t-1) + deltaB_u_t`

The sequential slow-forward fallback warning is expected.

Fast-kernel/GPU scientific execution is not authorized.

## 19. Frozen scientific layer

The only confirmatory scientific layer is:

`layer 23`

This layer was prospectively frozen before K2S/K2R outcomes and is retained unchanged.

K3T may use all 24 layers during synthetic instrumentation validation.

During scientific execution, implementation may technically capture all 24 layers if required by the frozen instrumentation helper, but:

- only layer 23 may be serialized as scientific metric output;
- no layer 0-22 scientific endpoint may be tested;
- no layer scan may be used for promotion;
- no alternate favorable layer may replace layer 23.

## 20. Synthetic instrumentation validation

Before any K3T scientific-population recurrent-state read, implementation validation must establish on synthetic non-study text:

- authenticated checkpoint reconstruction;
- strict state-dict load;
- canonical encoder fingerprint;
- raw encoder fingerprint;
- exact recurrence-source binding;
- direct native recurrent-state capture;
- float32 recurrent-state dtype;
- finite recurrent states;
- correct recurrent-state shape;
- fresh-state isolation;
- trace-on versus trace-off ordinary-logit exact noninterference;
- exact causal common-prefix state identity across independent branches;
- CPU sequential recurrence.

Synthetic validation may use model forward.

It must not use any K3T generated_fact_901 through generated_fact_1236 recurrent state.

## 21. Scientific-execution boundary

This preregistration does **not** authorize K3T scientific recurrent-state execution.

Before separate execution authority exists:

- K3T candidate materialization is allowed state-blind;
- tokenizer-only feasibility checks are allowed;
- synthetic non-study instrumentation validation is allowed;
- K3T population model forward is forbidden;
- K3T population recurrent-state read is forbidden;
- K3T scientific output artifacts are forbidden.

The scientific execution path must fail closed without a separately frozen K3T execution authority.

## 22. Primary R kinematics

For a branch b at layer 23 define velocity:

`V_(p+k)^b = S_(p+k)^b - S_(p+k-1)^b`

for:

`k = 1,...,8`

Define speed:

`speed_(p+k)^b = ||V_(p+k)^b||_F`

using the raw Euclidean/Frobenius norm.

Define branch mean speed:

`R_b = (1/8) * sum_(k=1..8) speed_(p+k)^b`

No normalization, whitening, learned projection, hidden-state head, or alternative norm is permitted.

## 23. Recipient-level pair-specificity

For recipient i define signed matched and swapped correction/control differences:

`Delta_R_matched(i) = R(M_corr(i)) - R(M_ctrl(i))`

`Delta_R_swapped(i) = R(S_corr(i)) - R(S_ctrl(i))`

Define R pair-specificity:

`X_R(i) = |Delta_R_matched(i)| - |Delta_R_swapped(i)|`

Positive X_R means recipient-matched correction/control assignment produces greater absolute R separation than the reciprocal swapped assignment.

No signed-delta alternative may replace this frozen absolute-separation specificity measure.

## 24. Reciprocal-block R statistic

For reciprocal block b containing items a and c define:

`B_R(b) = (X_R(a) + X_R(c)) / 2`

There are exactly:

`168`

primary reciprocal blocks.

The reciprocal block is the confirmatory statistical unit.

Item-level observations are not independent inferential units.

No block may be:

- deleted;
- replaced;
- reweighted;
- duplicated;
- selected by outcome;
- selected by phase after outcome;
- selected by p;
- selected by correction source.

## 25. Sole confirmatory hypothesis

K3T has exactly one confirmatory endpoint and one directional hypothesis.

Primary endpoint:

`R`

Frozen expected direction:

`B_R > 0`

Scientific interpretation:

the exact phase-balanced claim-disjoint K3T population exhibits positive pair-specific layer-23 mean-speed geometry under the same matched-versus-swapped construction used in K2R and K3C.

There is no confirmatory D endpoint.

There is no confirmatory DISP endpoint.

There is no confirmatory P endpoint.

There is no K3T mechanism endpoint.

## 26. Primary statistical test

Use all 168 reciprocal block values B_R.

Required:

`n_valid = 168`

R is mathematically defined for every valid finite state window.

Any undefined R block is a scientific-integrity failure rather than ordinary missing data.

Zeros are allowed.

Define:

`n_eff = n_positive + n_negative`

Zeros are excluded from n_eff.

Promotion floor:

`n_eff >= 30`

Use a two-sided exact sign test over positive versus negative nonzero B_R values.

Family size:

`m = 1`

Therefore:

`adjusted_p = raw_p`

Alpha:

`0.05`

Define rank-biserial sign effect:

`effect_R = (n_positive - n_negative) / n_eff`

No asymptotic normal approximation is permitted for the primary p-value.

## 27. Frozen primary verdict

If all integrity requirements pass, `n_eff >= 30`, the exact two-sided sign-test p-value is <= 0.05, and:

`effect_R > 0`

then:

`R_PHASE_BALANCED_TRANSPORTABILITY_SIGNAL_REPLICATED`

If all integrity requirements pass, `n_eff >= 30`, the exact two-sided sign-test p-value is <= 0.05, and:

`effect_R < 0`

then:

`R_PHASE_BALANCED_TRANSPORTABILITY_SIGNAL_CONTRADICTED`

Otherwise:

`R_PHASE_BALANCED_TRANSPORTABILITY_NOT_ESTABLISHED`

A zero effect cannot support or contradict the hypothesis.

## 28. Meaning of a positive K3T result

Even if K3T returns:

`R_PHASE_BALANCED_TRANSPORTABILITY_SIGNAL_REPLICATED`

the broader statement:

`R_CROSS_CLAIM_TRANSPORTABILITY_ESTABLISHED`

does **not** automatically become true.

K3C remains a valid prospective non-replication for R.

A positive K3T result would establish only that a prospectively phase-balanced new claim slice reproduces the positive R signal.

The multi-slice evidence would then be heterogeneous:

- K2R positive;
- K3C not established;
- K3T phase-balanced positive.

That pattern may motivate a later separately preregistered replication or formal heterogeneity study.

It does not erase K3C.

## 29. Meaning of a null K3T result

If K3T returns:

`R_PHASE_BALANCED_TRANSPORTABILITY_NOT_ESTABLISHED`

then phase balancing has not restored a confirmatory positive R signal on this prospective slice.

The existing conclusion:

`R_CROSS_CLAIM_TRANSPORTABILITY_NOT_ESTABLISHED`

remains.

No favorable phase, p-range, correction-source subset, block family, or alternate layer may rescue the primary result.

## 30. Meaning of an opposite K3T result

If K3T returns:

`R_PHASE_BALANCED_TRANSPORTABILITY_SIGNAL_CONTRADICTED`

then the new phase-balanced population provides significant opposite-direction evidence for R.

This strengthens the conclusion that R is not a stable positive cross-claim endpoint under the tested controlled-generator regime.

It still does not establish a causal mechanism.

## 31. Frozen reference diagnostics

For implementation reuse and integrity only, the same branch state windows may also produce the already-defined quantities:

- D mean turning;
- displacement;
- P efficiency;
- path length.

These are:

`REFERENCE_DIAGNOSTICS_ONLY`

They have no confirmatory K3T p-values.

They have no Holm family.

They cannot modify the R verdict.

They cannot be used to define a three-of-four or any other composite success rule.

Any future inferential use requires a separate successor preregistration.

## 32. Frozen phase metadata

Every K3T candidate artifact must preserve:

- global template index;
- generator phase class;
- cycle-in-slice;
- correction-source intervention.

These fields are construction/provenance metadata.

They are not confirmatory stratification variables.

No phase-specific, cycle-specific, source-specific, lexical-factor-specific, or p-specific inferential analysis may affect the K3T primary verdict.

## 33. Scientific artifact contract

A future authorized K3T scientific execution must emit immutable artifacts sufficient for independent reconstruction.

At minimum:

- generated source JSONL;
- candidate pool JSONL;
- reciprocal mapping JSON;
- item-level scientific metrics JSONL;
- block-level R metrics JSONL;
- primary R statistics JSON;
- scientific integrity JSON;
- manifest JSON;
- report markdown;
- SHA256SUMS.

The item-level artifact must preserve enough information to reconstruct:

- exact recipient/donor mapping;
- p;
- exact branch token IDs or cryptographically bound branch inputs;
- branch R values;
- matched and swapped signed R contrasts;
- X_R;
- state-window hashes;
- prefix-state identity evidence.

The block artifact must permit exact independent reconstruction of every B_R.

## 34. Scientific integrity requirements

The authorized run must fail closed unless all required integrity checks pass.

Required scientific checks include:

- 336 exact candidates;
- 168 reciprocal blocks;
- exact candidate SHA;
- exact mapping SHA;
- exact prior-population disjointness;
- exact phase count: 2 per phase;
- exact phase/source balance;
- exact tokenizer feasibility;
- exact checkpoint identity;
- exact encoder fingerprints;
- exact HF revision;
- Transformers 5.12.1;
- exact recurrence-source SHA;
- CPU sequential recurrence;
- exact common-prefix token identity;
- exact common-prefix native-state identity through p;
- finite layer-23 states;
- fresh independent branch forwards;
- no task-head based selection.

Any scientific-integrity failure prevents a substantive R verdict.

## 35. Historical result comparison

K2R and K3C values may be printed in the final K3T report as frozen historical references.

They are not part of the K3T primary hypothesis test.

No outcome-dependent distance-to-K2R or distance-to-K3C threshold is permitted.

No pooled post-hoc meta-analysis is authorized by this preregistration.

## 36. Anti-rescue restrictions

K3T does not permit:

- changing the `[900:1236]` slice;
- replacing failed or inconvenient claims;
- searching another 336-item slice;
- changing the phase definition after execution;
- selecting favorable phase classes;
- selecting favorable cycle;
- selecting favorable correction source;
- selecting favorable prefix lengths;
- selecting favorable d-p classes;
- changing stable-ID sorting;
- changing reciprocal pairing;
- changing W=8;
- changing layer 23;
- changing raw Frobenius speed;
- replacing R with D, DISP, P, or path length;
- changing `X_R = |Delta_matched| - |Delta_swapped|`;
- item-level pseudo-replication;
- dropping zero blocks;
- changing from two-sided to one-sided testing;
- adding an effect-size threshold after outcome inspection;
- using historical K2R/K3C outcomes to choose a favorable K3T subgroup;
- mechanistic interpretation of any reference diagnostic;
- K3C rerun;
- K4 execution.

## 37. Failure recovery

Scientific execution is not authorized by this preregistration.

When a later one-run execution authority is consumed:

- an unfavorable scientific result does not authorize rerun;
- a successful scientific run does not authorize replication;
- output-path conflict is a blocker before execution starts;
- provenance/hash/runtime mismatch is a blocker before scientific state read;
- any failure after scientific-population recurrent-state execution begins must be treated as an execution-consumption event unless a separately frozen recovery authority establishes otherwise;
- no silent second attempt is permitted.

## 38. Implementation authority after preregistration freeze

After this preregistration is committed with exact bytes, bounded K3T implementation is authorized only for:

- deterministic population materialization;
- exact provenance binding;
- tokenizer feasibility;
- phase-balance validation;
- claim-disjointness validation;
- reciprocal mapping;
- synthetic instrumentation validation;
- exact R kinematics;
- exact block construction;
- exact sign-test statistics;
- artifact writing and validation code;
- fail-closed scientific authority gates.

Implementation must not read K3T population recurrent states during implementation validation.

Implementation must not expose an executable K3T scientific path without separate authority.

## 39. Execution authority state

`K3T_PREREGISTRATION_DRAFTED = YES`

`K3T_IMPLEMENTATION_AUTHORIZED_AFTER_FREEZE = YES`

`K3T_SYNTHETIC_INSTRUMENTATION_VALIDATION_AUTHORIZED_AFTER_FREEZE = YES`

`K3T_STATE_BLIND_POPULATION_VALIDATION_AUTHORIZED_AFTER_FREEZE = YES`

`K3T_SCIENTIFIC_RECURRENT_STATE_EXECUTION_AUTHORIZED = NO`

`K3T_ONE_RUN_EXECUTION_AUTHORITY_EXISTS = NO`

`K3C_RERUN_AUTHORIZED = NO`

`K4_EXECUTION_AUTHORIZED = NO`

A separate exact execution authority must be frozen after K3T implementation and validation before any generated_fact_901 through generated_fact_1236 model forward or recurrent-state read is permitted.

## 40. Frozen prospective population audit identity

The state-blind population audit used to freeze K3T construction had script SHA256:

`994c4e8e84cee0d1c461af3aa7340fccb9f1ea9b8dc037249ff537f14f9d1a07`

It established:

`SCIENTIFIC_EXECUTION=NO`

`MODEL_FORWARD=NO`

`RECURRENT_STATE_READ=NO`

`PRIOR_SCIENTIFIC_OUTCOME_READ=NO`

and:

`K3T_STATE_BLIND_PHASE_BALANCED_POPULATION_AUDIT=PASS`

Frozen outputs:

Generator SHA256:

`4e9798591fbfffb6d15ea9b2f8cf5cd804a9e76713b9f6b13b354fe6ce93aa5c`

Generator Git blob:

`baee23a9f71333125f4a8735c2c92d20cab7eb4f`

Global template slice:

`[900:1236]`

Pair IDs:

`generated_fact_901` through `generated_fact_1236`

Items:

`336`

Blocks:

`168`

Phase period:

`168`

Phase classes:

`168`

Occurrences per phase:

`2`

Correction-source counts:

`{"none":168,"polarity_flip":168}`

Generated source rows:

`4368`

Generated-source canonical SHA256:

`fb699bcc99e00b8c437fd49c204345933d61c69215af853746bc4639545c7400`

Candidate-pool canonical SHA256:

`d95d245e358ff497ea50b95e4f54d1192be64d09fec2c06538fc15f75b09ef70`

Reciprocal-mapping canonical SHA256:

`1c458054540ac0d38857cb3f55595e0b286428225ef11ed22059981dfd1e28ad`

Claim overlap against K2W:

zero on pair ID, claim SHA, and claim text.

Claim overlap against K2R/K3:

zero on pair ID, claim SHA, and claim text.

Claim overlap against K3C:

zero on pair ID, claim SHA, and claim text.

Tokenizer matched d-p:

`{"2":168,"3":168}`

Tokenizer swapped d-p:

`{"2":168,"3":168}`

All 336 matched and 336 swapped constructions passed exact-prefix and W=8 feasibility.

## 41. Final preregistered question

K3T asks exactly:

**On the frozen claim-disjoint `[900:1236]` population with exactly two occurrences of every 168-class nonnumeric lexical generator phase and exact phase-by-correction-source balance, is layer-23 reciprocal-block R pair-specificity significantly positive under the unchanged K2R matched-versus-swapped W=8 native-state geometry?**

No broader scientific question is authorized in K3T.
