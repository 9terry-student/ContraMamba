# K0-RVG-P2 One-Time Read-Only Real-Artifact Static Diagnostic Execution Authority Candidate

**Status:** one-time read-only static-diagnostic execution authority candidate.

**Date:** 2026-09-12

**Immediate parent / frozen P2 implementation validation-readiness commit:**

`88d021a376ed41bbe7277c164cd437ec2d322e02`

**Frozen P2 implementation validation-readiness report:**

`reports/longterm_k0_rvg_p2_static_diagnostic_implementation_validation_readiness_report_candidate.md`

**Frozen readiness report SHA256:**

`9f21835c9f90aba62fc007a95eee09bb3ec0aa54b9b4095de73fd0f5f2f660bc`

**Frozen readiness report Git blob:**

`50b01fe172c3dda3b5ff5307817dee49f093d7bd`

**Frozen P2 implementation authority commit:**

`95709b318bb4b4454d57edf52266860ded9e9da3`

**Frozen P2 implementation commit:**

`8e366761a92392ac0adc3c6550bccdaf791f14cf`

This document authorizes exactly one provenance-distinct, read-only K0-RVG-P2 static diagnostic execution over the already validated P1 result artifacts and the frozen P0 state-blind archive.

It does not authorize a P1 rerun.

It does not authorize model construction, model forward, checkpoint loading, tokenizer loading, network access, observer hooks, recurrent-tensor capture, logits read, training, evaluation, causal intervention, learned geometry, layer/window search, or K4.

The historical K-series A/B fork remains context only and is not reopened.

## 1. Active stage

`ACTIVE_SCIENTIFIC_LINE = K0_RVG_RAW_NATIVE_VECTOR_GEOMETRY`

`ACTIVE_STAGE = K0_RVG_P2_REAL_ARTIFACT_STATIC_DEGENERACY_DIAGNOSTIC`

The frozen P1 scientific conclusion remains:

`P1_VALIDATED_SCIENTIFIC_CONCLUSION = RAW_NATIVE_VECTOR_ORGANIZATION_NOT_ESTABLISHED`

P2 does not replace or rescue that result.

Its only purpose is to localize why the frozen P1 matched-versus-swapped endpoint contrasts were exactly zero for 335 of 336 items.

## 2. Readiness prerequisite

The frozen readiness report establishes:

`P2_CODE_CORRECTNESS = PASS_FOR_FROZEN_STATIC_DIAGNOSTIC_CONTRACT`

`P2_FOCUSED_TEST_CONTRACT = PASS_18`

`P2_SYNTHETIC_IMPLEMENTATION_VALIDATION = PASS`

`P2_REMOTE_STATIC_IMPLEMENTATION_REVIEW = PASS`

`P2_IMPLEMENTATION_PROVENANCE_VALID = YES`

`P2_IMPLEMENTATION_VALIDATED = YES`

`READY_FOR_P2_REAL_ARTIFACT_DIAGNOSTIC_EXECUTION_AUTHORITY_DRAFT = YES`

Therefore the bounded real-artifact static diagnostic may now be authorized.

## 3. Exact implementation binding

Authorized implementation:

`scripts/longterm_k0_rvg_p2_item_level_degeneracy_static_diagnostic.py`

Implementation commit:

`8e366761a92392ac0adc3c6550bccdaf791f14cf`

Implementation SHA256:

`51704aff68ddd24d8a71805085f1352be73f90c2d960757de66179940a51612e`

Implementation Git blob:

`c96eae5303b0a6d3fd7f237875d6807c077a7d77`

Authorized implementation test:

`tests/test_longterm_k0_rvg_p2_item_level_degeneracy_static_diagnostic.py`

Test SHA256:

`f37c08eb2dcd87edd48e1b7692a743ccce765ac4d6b6d112aaea92e5614b4020`

Test Git blob:

`ddbb0b10168b5ce6a75b611ca49d5925c6e9e719`

Any implementation or test drift blocks execution.

## 4. Exact validated P1 input directory

The only authorized P1 result directory is:

`C:\Users\Home1\Desktop\ContraMamba-K0-RVG-P1-Runs\p1-r2-scientific-db8f72b-v1`

The directory must contain exactly the following six files and no other files:

`item_metrics.jsonl`

`block_metrics.jsonl`

`endpoint_summary.json`

`recurrence_audit.json`

`state_hash_audit.jsonl`

`execution_manifest.json`

Exact SHA256 binding:

`item_metrics.jsonl = 7a8ac4cb347a1a64c2dd69653a4bc575641679667534a89f42d44e257f9550e9`

`block_metrics.jsonl = c00197ab3c93ee2bb0d3951dbb47b452a0f0aa2ba920219c28707ab544e50530`

`endpoint_summary.json = 5138fb7456e8626093753e188a3e334d79c91e5d9bc2a465073e4ae43823ec0d`

`recurrence_audit.json = f97dd5a3d934eccc91fde8430ebfea8c67018b7ef2af6a940e5adf968c4cdd5f`

`state_hash_audit.jsonl = 8b9d68a81f245b2dce46238dd915a9cf6eb0a8032556fb44b92a9c8cfce2a274`

`execution_manifest.json = 20bd491dbebaa3a882e7fce02d1ed039fec036bec08d7bd01287f109e6d74b26`

No original failed P1 output, copied-and-edited P1 output, partial output, repaired output, or alternate result directory is authorized.

## 5. Exact frozen P0 archive binding

Authorized P0 archive:

`reports/longterm_k0_rvg_p0_state_blind_provisioning_421d798_v1`

Exact SHA256 binding:

`candidate_pool.jsonl = 743657411af4e143931e4d2c79f17043134bff3a370d30910588504c7f19f246`

`generated_source.jsonl = 8137c0020a040faaf0c6be833b123e143dc5a0a09ae092e8e99530bb8671c1bf`

`phase_pair_mapping.json = c5e1fa2ac946d153821896d5153354fae6e17038a55be87b3d31ab43d2921eda`

`token_contracts.jsonl = 6eb006f7deca28affa73318887421879e1279fd6897fab857b460873add9a998`

`provisioning_manifest.json = feab9c60e3546ace3258f068e38d5bb577fc63b803b95349379fa8b4db425e52`

`validation_report_candidate.md = ae2fe4d1db13e1765415eab9c95263aec618fccf9299fe93fad0c8e31141b73a`

P0 files are read only for frozen state-blind provenance/construction diagnostics.

No regeneration or replacement is permitted.

## 6. P1 provenance facts that must revalidate before interpretation

The diagnostic must fail closed unless all of the following revalidate:

`P1_ITEM_COUNT = 336`

`P1_BLOCK_COUNT = 168`

`P1_STATE_HASH_ROW_COUNT = 12096`

`P1_RUNTIME_HEAD = db8f72bc8121a06a5dc9120611fa56b36eaec26f`

`P1_IMPLEMENTATION_COMMIT = 50a1daa781e47d1c0f1ba158beb445878e049a65`

`P1_SCIENTIFIC_EXECUTION_AUTHORITY_COMMIT = db8f72bc8121a06a5dc9120611fa56b36eaec26f`

`P1_OVERALL_VERDICT = RAW_NATIVE_VECTOR_ORGANIZATION_NOT_ESTABLISHED`

`P1_EXACT_ZERO_ITEM_COUNT_TURNING = 335`

`P1_EXACT_ZERO_ITEM_COUNT_RESPONSE_COHERENCE = 335`

All required JSON/JSONL inputs must pass the frozen canonical-serialization contract.

## 7. Authorized reads

This execution may read only:

1. the exact P2 execution-authority report itself;
2. repository/Git metadata required to authenticate the frozen P2 implementation and authority;
3. the exact six validated P1 files in the authorized P1 result directory;
4. the exact six frozen P0 files in the authorized P0 archive.

`P2_P1_VALIDATED_ARTIFACT_READ_AUTHORIZED = YES`

`P2_P0_FROZEN_ARCHIVE_READ_AUTHORIZED = YES`

Reading `state_hash_audit.jsonl` means reading persisted SHA256 strings only.

It does not authorize reconstructing or rereading recurrent tensors.

## 8. Authorized computations

Exactly the frozen P2 implementation computations are authorized.

### Q1 — turning-component localization

Classify exact persisted equality into:

`TURNING_COMPONENTWISE_IDENTITY`

`TURNING_EQUAL_BY_OFFSET_CANCELLATION`

`TURNING_DIFFERENT`

`TURNING_INVALID`

### Q2 — coherence-summary localization

Classify exact persisted equality into:

`COHERENCE_EXACT_IDENTITY`

`COHERENCE_DIFFERENT`

`COHERENCE_INVALID`

### Q3 — endpoint-supporting state-hash identity

For every item, compare exactly 72 persisted SHA256 values:

`2 branch-role pairs * 9 event-relative coordinates * 4 tensor fields`

using:

`matched_corr <-> swapped_corr`

and:

`matched_ctrl <-> swapped_ctrl`

at relative coordinates:

`-1, 0, 1, 2, 3, 4, 5, 6, 7`

for:

`S_prev_sha256`

`G_sha256`

`W_sha256`

`S_post_sha256`

### Q4 — frozen construction identity

Compare only the frozen P0 state-blind construction metadata/text available under the frozen implementation contract.

### Q5 — persisted diagnostic-summary identity

Compare only:

`response_norm_mean`

`carry_response_norm_mean`

`write_response_norm_mean`

`write_total_cosine_mean`

`carry_total_cosine_mean`

`write_carry_cosine_mean`

No new endpoint, distance, tolerance, learned representation, or tensor geometry is authorized.

## 9. Authorized cross-level localization

For exact-zero primary endpoints, the frozen labels are authorized:

`STATE_IDENTITY_EXPLAINS_ENDPOINT_IDENTITY`

`DISTINCT_STATE_ENDPOINT_FUNCTIONAL_COLLAPSE`

`DISTINCT_COMPONENTS_EQUAL_BY_TURNING_CANCELLATION`

These are descriptive localization labels only.

They are not causal conclusions.

A SHA256 difference proves byte nonidentity only.

It does not establish numerical distance, biological meaning, mechanistic importance, or scientific effect size.

## 10. Exact output directory

The only authorized output directory is:

`C:\Users\Home1\Desktop\ContraMamba-K0-RVG-P2-Runs\p2-static-diagnostic-88d021a-v1`

The output directory must not exist before execution.

No resume is authorized.

No overwrite is authorized.

No reuse of a partial directory is authorized.

## 11. Exact output artifact set

Exactly one output artifact is authorized:

`p2_degeneracy_diagnostic.json`

Required schema:

`k0-rvg-p2-item-level-degeneracy-static-diagnostic-v1`

No second artifact, log artifact, tensor dump, projection, embedding, checkpoint, cache, or model output is part of the authorized P2 result set.

Ordinary terminal stdout is not a scientific result artifact.

## 12. Execution environment

Execution is local and CPU-only in the sense that no GPU computation is needed or authorized.

No Kaggle execution is required or authorized.

No model process is authorized.

No external network access is required or authorized.

No HF snapshot resolution is authorized.

No tokenizer construction is authorized.

No checkpoint read is authorized.

The diagnostic is a deterministic file-level static analysis.

## 13. One-time execution contract

Exactly one real-artifact P2 diagnostic attempt is authorized.

`P2_REAL_ARTIFACT_DIAGNOSTIC_ATTEMPT_LIMIT = 1`

`AUTOMATIC_RERUN_AUTHORIZED = NO`

`RESUME_AUTHORIZED = NO`

`OVERWRITE_AUTHORIZED = NO`

If execution fails before producing the complete authorized artifact, do not rerun automatically.

Preserve the failure record and return to authority review.

If the output directory exists for any reason before the intended first attempt, execution is blocked pending review.

## 14. Fail-closed conditions

Execution must stop on any of:

- branch/HEAD authority mismatch;
- implementation commit mismatch;
- implementation SHA256 or Git blob mismatch;
- test SHA256 or Git blob mismatch;
- execution-authority authentication failure;
- P1 artifact-set mismatch;
- P1 SHA256 mismatch;
- P0 SHA256 mismatch;
- canonical JSON/JSONL failure;
- item/block/state-hash count mismatch;
- frozen P1 provenance mismatch;
- frozen P1 verdict mismatch;
- frozen 335/336 exact-zero-count mismatch;
- duplicate or missing item index;
- unexpected branch role;
- missing event-relative state-hash coordinate;
- unexpected hash comparison state;
- pre-existing output directory;
- attempt to create more than the one authorized result artifact;
- any request for model/checkpoint/tokenizer/network access;
- any request to read raw recurrent tensors.

## 15. Interpretation boundary

A successful P2 diagnostic may establish only where the already observed P1 endpoint identity occurs in the frozen evidence chain.

Examples of allowed later interpretations:

- endpoint-supporting matched/swapped state snapshots are byte-identical under persisted hashes;
- endpoint-supporting state snapshots differ in at least one persisted hash while the frozen endpoint is identical;
- turning equality is componentwise;
- turning equality is produced by exact offset cancellation;
- descriptive carry/write response summaries differ while the primary frozen endpoint is identical;
- matched/swapped frozen construction metadata/text are identical or different.

The execution itself does not establish a scientific causal explanation.

It does not authorize selecting a new geometry based on these results.

## 16. Explicitly forbidden actions

The following remain forbidden:

- P1 rerun;
- model forward;
- recurrent-state capture or reread;
- checkpoint loading;
- tokenizer/model loading;
- network access;
- logits read;
- training;
- evaluation of a new model;
- causal intervention;
- changing layer 23;
- changing W=8;
- changing matched/swapped construction;
- changing event anchors;
- tolerance retuning;
- approximate state comparison;
- PCA/SVD/whitening/Mahalanobis/probe/learned geometry;
- post-result subgroup selection;
- promotion of carry/write diagnostics to causal status;
- historical A/B fork reopening;
- K4 execution.

## 17. Exact execution command after authority freeze

After this exact authority is committed and pushed, the authorized command is:

```powershell
Set-Location "C:\Users\Home1\Desktop\ContraMamba-K"

python scripts/longterm_k0_rvg_p2_item_level_degeneracy_static_diagnostic.py execute-real `
  --repo-root "C:\Users\Home1\Desktop\ContraMamba-K" `
  --p1-dir "C:\Users\Home1\Desktop\ContraMamba-K0-RVG-P1-Runs\p1-r2-scientific-db8f72b-v1" `
  --execution-authority "reports/longterm_k0_rvg_p2_real_artifact_static_diagnostic_execution_authority_candidate.md" `
  --output-dir "C:\Users\Home1\Desktop\ContraMamba-K0-RVG-P2-Runs\p2-static-diagnostic-88d021a-v1"
```

This command must not be run before the authority report is committed and pushed.

## 18. Post-execution evidence to return

After the one authorized execution, return the complete terminal JSON output and then run read-only:

```powershell
Get-ChildItem -LiteralPath "C:\Users\Home1\Desktop\ContraMamba-K0-RVG-P2-Runs\p2-static-diagnostic-88d021a-v1" |
    Select-Object Name, Length

Get-FileHash -LiteralPath "C:\Users\Home1\Desktop\ContraMamba-K0-RVG-P2-Runs\p2-static-diagnostic-88d021a-v1\p2_degeneracy_diagnostic.json" -Algorithm SHA256 |
    Format-List Path, Hash

git status --short
```

Do not interpret the diagnostic scientifically until artifact/provenance validity has been checked.

## 19. Authority markers

`HISTORICAL_AB_FORK_AUTHORITY = CONTEXT_ONLY_SUPERSEDED_BY_K0_RVG`

`P2_READINESS_COMMIT = 88d021a376ed41bbe7277c164cd437ec2d322e02`

`P2_READINESS_REPORT_SHA256 = 9f21835c9f90aba62fc007a95eee09bb3ec0aa54b9b4095de73fd0f5f2f660bc`

`P2_READINESS_REPORT_GIT_BLOB = 50b01fe172c3dda3b5ff5307817dee49f093d7bd`

`P2_IMPLEMENTATION_COMMIT = 8e366761a92392ac0adc3c6550bccdaf791f14cf`

`P2_IMPLEMENTATION_SHA256 = 51704aff68ddd24d8a71805085f1352be73f90c2d960757de66179940a51612e`

`P2_IMPLEMENTATION_GIT_BLOB = c96eae5303b0a6d3fd7f237875d6807c077a7d77`

`P2_TEST_SHA256 = f37c08eb2dcd87edd48e1b7692a743ccce765ac4d6b6d112aaea92e5614b4020`

`P2_TEST_GIT_BLOB = ddbb0b10168b5ce6a75b611ca49d5925c6e9e719`

`P2_REAL_ARTIFACT_DIAGNOSTIC_EXECUTION_AUTHORIZED = YES`

`P2_P1_VALIDATED_ARTIFACT_READ_AUTHORIZED = YES`

`P2_P0_FROZEN_ARCHIVE_READ_AUTHORIZED = YES`

`P2_REAL_ARTIFACT_DIAGNOSTIC_ATTEMPT_LIMIT = 1`

`AUTOMATIC_RERUN_AUTHORIZED = NO`

`RESUME_AUTHORIZED = NO`

`OVERWRITE_AUTHORIZED = NO`

`SCIENTIFIC_MODEL_FORWARD_AUTHORIZED = NO`

`SCIENTIFIC_RECURRENT_STATE_READ_AUTHORIZED = NO`

`LOGITS_READ_AUTHORIZED = NO`

`P1_SCIENTIFIC_RERUN_AUTHORIZED = NO`

`TRAINING_AUTHORIZED = NO`

`CAUSAL_INTERVENTION_AUTHORIZED = NO`

`LEARNED_OR_TUNED_GEOMETRY_AUTHORIZED = NO`

`KAGGLE_EXECUTION_AUTHORIZED = NO`

`K4_EXECUTION_AUTHORIZED = NO`

`NEXT_BOUNDARY = K0_RVG_P2_ONE_TIME_STATIC_DIAGNOSTIC_EXECUTION`

This document becomes the frozen one-time P2 real-artifact static diagnostic execution authority only after this exact document is committed and pushed as the immediate one-file child of:

`88d021a376ed41bbe7277c164cd437ec2d322e02`
